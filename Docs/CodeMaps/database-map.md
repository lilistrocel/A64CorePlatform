# Database Map

> Generated: 2026-08-21 15:36 UTC  
> Source: MongoDB `mapper_nodes` (node_type=db_model, layer=model)

## Overview

A64 Core Platform uses MongoDB 7.0 as primary database.
This map covers all collections, document schemas, and inter-collection relationships.

**Related Maps:** [module-map.md](module-map.md) | [service-map.md](service-map.md)

## Collections by Module (208 models)

### Module: `ai_analytics`

| Collection/Model | File | Description |
|------------------|------|-------------|
| `ChatQueryResponse` | `src/modules/ai_analytics/models/chat.py:110` | AI chat request/response models with query info, visualization suggestions, cost info. | ChatQueryRequest, ChatQueryResponse, SchemaResponse |
| `ai_query_log` | `src/services/database.py` | MongoDB collection: ai_query_log - tracks AI query usage and costs (Vertex AI) |

### Module: `ai_assistant`

| Collection/Model | File | Description |
|------------------|------|-------------|
| `AssistantCostLog` | `src/modules/ai_assistant/models/cost_log.py:14` | Per-call cost record written to ai_assistant_cost_log: user_id, conversation_id, input/output/cache_creation/cache_read token counts, computed cost_usd, tool_calls, model, scope, created_at, duration_seconds. | AssistantCostLog |
| `ChatRequest` | `src/modules/ai_assistant/models/chat_request.py:52` | Request body models for POST /ai/assistant/chat. ChatRequest: message (1-8000 chars), optional conversation_id (null = new), context. ChatContext: optional farm_id, optional block_id, scope. ChatScope is a str Enum - BLOCK | FARM | GLOBAL - and selects which context tier context_composer builds. | ChatRequest, ChatContext, ChatScope |
| `Conversation` | `src/modules/ai_assistant/models/conversation.py:38` | Conversation document stored in ai_assistant_conversations: conversation_id (UUID str), user_id, title, ordered messages, ChatContext snapshot, created_at, updated_at. Message mirrors the Anthropic messages API shape (role/content plus timestamp and metadata) so history can be replayed directly. MessageRole is a str Enum - USER | ASSISTANT. ConversationSummary is the lightweight list projection (message_count instead of messages) returned by GET /ai/assistant/conversations. | Conversation, ConversationSummary, Message, MessageRole |
| `ai_assistant_conversations` | `src/modules/ai_assistant/services/conversation_repository.py:28` | MongoDB collection holding AI assistant conversation history, one document per conversation with the full embedded message list. Capped per user at settings.AI_ASSISTANT_HISTORY_LIMIT (default 3) by oldest-first eviction on create. | conversation_id, user_id, title, messages, context, created_at, updated_at |
| `ai_assistant_cost_log` | `src/modules/ai_assistant/services/cost_tracker.py:121` | MongoDB collection of per-Claude-call token/cost records written by CostTracker.record(). Distinct from ai_query_log, which belongs to the ai_analytics Gemini path. | user_id, conversation_id, input_tokens, output_tokens, cache_creation_tokens, ca |

### Module: `core`

| Collection/Model | File | Description |
|------------------|------|-------------|
| `ChartWidgetData / StatWidgetData / WidgetDataResponse` | `src/models/dashboard.py:16` | CCM Dashboard widget schemas: chart widgets (line/bar/pie via ChartSeries for multi-series) and stat widgets (value + trend), the three data-source descriptors a widget can declare its origin as (ModuleDataSource, SystemDataSource, ExternalAPIDataSource), the CCMWidget definition itself, DashboardLayout/DashboardConfig for saved arrangements, and the BulkWidgetDataRequest/Response envelope behind POST /dashboard/widgets/bulk (partial-failure tolerant, up to 50 ids). Consumed by core.api.dashboard and core.service.dashboard_service — note that service currently returns randomly generated placeholder data, so these models describe a contract that is not yet backed by real metrics. | ChartSeries, ChartWidgetData, StatWidgetData, ModuleDataSource, SystemDataSource |
| `DeploymentSettingItem / DeploymentSettingsResponse / DeploymentSettingsPatchRequest` | `src/models/deployment_settings.py:15` | Request/response schemas for GET and PATCH /api/v1/admin/deployment-settings, the UI over the env -> platform_settings-db -> unset resolution in core.service.deployment_settings_service. DeploymentSettingItem reports each managed key's `source` ('env' | 'db' | 'unset') and `editable` (False whenever source is 'env' — an env-pinned key cannot be changed through the API). It carries `value` for ordinary keys but for SECRET keys populates only `isSet` plus `maskedHint` (last 4 characters). That secret set is now THREE, not two: T-925 added LABEL_PRINTER_API_KEY (the Brother QL-800 print API key) alongside CF_ACCESS_TEAM_DOMAIN and CF_ACCESS_AUD — see _SECRET_DEPLOYMENT_KEYS in api/v1/admin.py and _SECRET_KEYS in services/deployment_settings_service.py, and note there is deliberately no reveal endpoint for any of them (they are masked in the audit log too). DeploymentSettingsPatchRequest requires the acting super_admin's `currentPassword` so a hijacked session alone cannot repoint authentication, and `changes` must contain only the keys being modified — an env-pinned or unknown key is rejected 409/422. | DeploymentSettingItem, DeploymentSettingsResponse, DeploymentSettingsPatchReques |
| `IndustryType / Division / DivisionResponse` | `src/models/division.py:15` | Division = the second tenancy level below Organization, and the unit that core.middleware.division_context scopes queries to. IndustryType (vegetable_fruits, mushroom) is defined here and is what core.api.industries and the plugin system use for per-industry module scoping. A Division belongs to an organizationId and carries a divisionCode that must be unique within that org. DivisionSelectResponse is returned by POST /divisions/{id}/select, which persists user.defaultDivisionId so later requests context-switch without re-selecting. | IndustryType, DivisionBase, DivisionCreate, DivisionUpdate, Division, DivisionRe |
| `ModuleConfig / ModuleStatusResponse / PortAllocation` | `src/models/module.py:25` | Schema family for Docker-Compose-based module management (core.api.modules + core.service.module_manager / port_manager / proxy_manager). ModuleConfig is the install payload: image reference, license key, resource limits and container security profile. ModuleStatus / ModuleHealth are the runtime enums; ModuleStatusResponse carries the CPU/memory/uptime metrics scraped from Docker stats. ModuleAuditLog backs the 90-day-TTL module_audit_log collection that records every install/uninstall. PortAllocation and PortRange are PortManager's registry types for the 9001-19999 auto-allocation range. Distinct from the plugin system in src/core/plugin_system/ — this file is about installing external containers, not loading src/modules/* Python plugins. | ModuleStatus, ModuleHealth, ModuleConfig, ModuleInDB, ModuleResponse, ModuleList |
| `Organization / module config models` | `src/models/organization.py:1` | T-804: PublicInfoPageConfig — what a scanned genetics label may reveal to the public internet. Every show* flag (showMediumIngredients/showProtocolSteps/showOperatorName/showFacilityName) defaults to False, a deliberate privacy/trade-secret decision. Read directly by genetics.api.public to gate the anonymous response shape; ignored entirely by the authenticated shape. NOTE: only these module-config models are mapped here; the rest of organization.py is unmapped pending map_core_api. | PublicInfoPageConfig, PublicInfoPageConfigUpdate, OrganizationModules, Organizat |
| `UserMFA / MFABackupCode / MFAAuditLog` | `src/models/mfa.py:13` | Largely a parallel/legacy schema design that the live code does not use. UserMFA / MFABackupCode / MFAAuditLog describe separate user_mfa, mfa_backup_codes and mfa_audit_log collections which core.service.database dutifully creates indexes for — but core.service.mfa_service keeps ALL MFA state inline on the users document (mfaSecret, mfaBackupCodes, mfaPendingSecret, mfaLastUsedCounter), so those three collections stay empty on every deployment. The MFA request/response models here are also shadowed by same-named ones in core.model.user, which is what auth.py actually imports. The one type from this file wired into the live flow is MFALoginRequired, used by core.api.auth as the response for a pending MFA challenge. Treat the rest as unimplemented design intent, not as a description of stored data. | MFAMethod, MFAStatus, MFASetupResponse, MFAVerifyRequest, MFAEnableRequest, MFAD |
| `UserRole / UserCreate / UserResponse / TokenResponse / MFA* models` | `src/models/user.py:15` | The core user/auth Pydantic family and the single most widely imported model file in the backend. UserRole is the platform-wide role enum (base hierarchy super_admin/admin/moderator/user/guest plus finance roles accountant/finance_admin/auditor and purchasing roles procurement_officer/procurement_manager) — config/settings.py imports it purely to validate CF_ACCESS_DEFAULT_ROLE at boot, which makes this file an import dependency of the settings module. UserCreate and PasswordResetConfirm enforce password complexity (upper/lower/digit/special, 8-128 chars) with field validators. UserResponse is the shape every authenticated endpoint returns and that core.middleware.auth hand-constructs; it carries authProvider ('password' | 'cloudflare_access') and nameAutoDerived (True for Cloudflare-JIT users whose name was guessed from the email local-part, cleared once the user edits either name). Note the MFA* request/response models are duplicated between here and core.model.mfa — the ones wired into the live auth flow are mostly these. | UserRole, UserBase, UserCreate, UserUpdate, UserOrganizationAssignment, UserResp |
| `admin_audit_log` | `src/services/database.py:158` | Append-only trail of privileged actions: user-targeted admin actions (role change, activate/deactivate, forced MFA reset) via core.service.audit_log_service, deployment-settings changes with masked before/after via deployment_settings_service.update, tenant module toggles from the organizations endpoint, and the genetics cascade-purge / orphan-sweep routes. Every writer is best-effort — none rolls back the mutation if the audit insert fails. |
| `counters` | `src/modules/crm/services/customer/customer_repository.py` | MongoDB collection: counters - the older, cross-module code-sequence counter used by the non-document modules. Shape: {_id: <sequence name, e.g. 'vehicle_sequence'>, value: <int>} bumped via find_one_and_update($inc). Written by crm.customer_repository, hr.employee_repository, logistics.vehicle_repository and logistics.shipment_repository to mint customerCode / employeeCode / vehicleCode / shipmentCode. LIVE: 3 sequences. Distinct from document_counters, which serves the Wave 0 document chain. |
| `divisions` | `src/services/division_service.py:1` | Divisions scoped to an organization, each mapped to an IndustryType (vegetable_fruits / mushroom) and carrying a divisionCode unique per org. The divisionId here is what core.repository_base.DivisionScopedRepository auto-injects into every scoped query. Written by division_service. |
| `document_counters` | `src/core/documents/doc_number.py` | MongoDB collection: document_counters - atomic per-series sequence counters backing core.documents.doc_number.next_doc_number (_COUNTERS_COLLECTION; also _COUNTERS_COL in purchasing.document_service). Shape: {_id: <series key, e.g. docType+org+year>, counter: <int>} incremented with find_one_and_update($inc). LIVE: 16 counter docs. Only the _id index - which is exactly the uniqueness guarantee the pattern needs. |
| `document_headers` | `src/core/documents/doc_number.py` | MongoDB collection: document_headers - the Wave 0 generic purchasing/procurement document header shared by PR, PO, GR and AP invoice (default headers_collection in core.documents.doc_number; _HEADERS_COL in purchasing.document_service, purchasing_chain_reconciler and attachments.attachment_service). Fields: docId, docType, docNumber, organizationId, companyCode, vendorId/vendorCode/vendorName, currencyCode, paymentTermsCode, docDate, postingDate, dueDate, expectedDeliveryDate, requestedBy/requestedDate, issuedBy/issuedDate, department, urgency, approvalState + approvalRequestedFrom/At + approvalDecidedBy/At + approvalComment, status, subtotalNet/totalTax/totalGross, notes, baseDocId, deletedAt. finance_bridge.outbox_writer stamps outbox metadata onto it in the same transaction as the outbox insert. LIVE: 3 docs, NO indexes beyond _id - no module declares any. |
| `document_lines` | `src/core/documents/open_quantity.py` | MongoDB collection: document_lines - Wave 0 generic document line rows, one document_headers.docId to many lines (_LINES_COL in purchasing.document_service / purchasing_chain_reconciler; lines_collection argument to core.documents.open_quantity and document_links). Fields: lineId, docId, lineNumber, itemId/itemCode/itemName, description, quantity, openQuantity, closedQuantity, uom, unitPrice, discountPercent, taxCode/taxRate, lineNet/lineTax/lineGross, warehouseId, costCenterId, requestedVendorId, baseLineId (copy-from chain), notes, organizationId. The open/closed quantity pair is what core.documents.open_quantity mutates when a target document is issued. LIVE: 4 docs, NO indexes beyond _id. |
| `installed_modules` | `src/services/database.py:88` | Registry of Docker-Compose-installed modules. Indexes: unique module_name, status, health, installed_by_user_id, installed_at desc, updated_at desc, container_id. Written exclusively by core.service.module_manager. |
| `mfa_audit_log` | `src/services/database.py:140` | DECLARED BUT UNUSED, same story as user_mfa / mfa_backup_codes. Indexed by MongoDBManager (unique logId, ...) but no code path writes it; MFA-related admin actions land in admin_audit_log instead. |
| `mfa_backup_codes` | `src/services/database.py:127` | DECLARED BUT UNUSED, same story as user_mfa. Indexes exist (unique codeId, userId, (userId, codeHash), isUsed, (userId, isUsed), TTL) but mfa_service stores the 10 SHA-256-hashed backup codes on the users document. |
| `mfa_pending_tokens` | `src/services/auth_service.py:853` | Short-lived challenge tokens issued when a password login succeeds but MFA is still required; POST /auth/mfa/verify exchanges one for the real token pair. Created and consumed entirely inside auth_service (_issue_mfa_challenge / verify path) — note this collection is NOT declared in MongoDBManager._create_indexes, so it carries no indexes and no TTL. |
| `module_audit_log` | `src/services/database.py:97` | Audit trail for module install/uninstall/status operations. Indexes: module_name, operation, user_id, status, timestamp desc, plus a 90-day TTL index on timestamp. Written by core.service.module_manager, surfaced by GET /modules/audit-log. |
| `organizations` | `src/services/organization_service.py:1` | Top-level tenancy unit (unique slug). Holds the Wave 0 per-tenant modules sub-document: modules.financeEnabled (hides the Finance UI and gates the finance_bridge outbox writer) and modules.publicInfoPage (T-804 privacy config for scanned genetics labels). Written by organization_service; read by finance_bridge.tenant_flag and genetics.api.public. |
| `platform_settings` | `src/services/deployment_settings_service.py` | MongoDB collection: platform_settings - singleton document (_id: 'deployment') holding deployment-wide config: PUBLIC_BASE_URL/FRONTEND_URL identity and CF_ACCESS_* Cloudflare Access settings. Written only via deployment_settings_service.update(); resolution order for any managed key is env var -> this document -> unset (env acts as a lock making the DB value non-editable). |
| `port_registry` | `src/services/port_manager.py:49` | Allocation table for the 9001-19999 module port range. Indexes created by PortManager itself (not by MongoDBManager): unique port, module_name, status, and a compound (module_name, status). Written only by core.service.port_manager, which rolls back partial allocations on failure. |
| `refresh_tokens` | `src/services/database.py:72` | Issued refresh tokens for rotation and revocation. Indexes: unique tokenId, userId, and a TTL index on expiresAt so expired rows self-delete. Written by auth_service (issue/rotate/logout) and user_service (revoke-all on deactivate/delete). |
| `user_mfa` | `src/services/database.py:118` | DECLARED BUT UNUSED. MongoDBManager._create_indexes builds indexes here (unique mfaId, unique userId, isEnabled, createdAt/updatedAt desc) and src/models/mfa.py defines a matching schema, but core.service.mfa_service keeps all live MFA state inline on the users document instead. Nothing reads or writes this collection today — it is a parallel/legacy design that was indexed but never wired up. |
| `users` | `src/services/database.py:66` | Primary account collection. Indexes declared by MongoDBManager._create_indexes: unique email, unique userId, role, createdAt desc. Also carries the live MFA state inline (mfaSecret, mfaBackupCodes, mfaPendingSecret, mfaLastUsedCounter) rather than in the separate user_mfa collection, plus defaultDivisionId (set by division select), organizationId, authProvider and nameAutoDerived. Written by auth_service, user_service, mfa_service and division_service; read by deployment_settings_service for the guardrail-c password check. |
| `verification_tokens` | `src/services/database.py:79` | One-time tokens for email verification and password reset. Indexes: unique tokenId, userId, email, tokenType, plus a TTL index on expiresAt. Written and consumed by auth_service. |

### Module: `crm`

| Collection/Model | File | Description |
|------------------|------|-------------|
| `Customer` | `src/modules/crm/models/customer.py:68` | Customer model with type, status, address, and contact info. | Customer, CustomerCreate, CustomerType, CustomerStatus |
| `customers` | `src/modules/crm/services/customer/customer_repository.py` | MongoDB collection: customers - CRM customer records (individual and business) |

### Module: `farm_manager`

| Collection/Model | File | Description |
|------------------|------|-------------|
| `AgriWeatherData` | `src/modules/farm_manager/models/weather.py:269` | Combined agricultural weather payload: current conditions, AgriWeatherForecast days and derived AgriculturalInsights. Response-only. | AgriWeatherForecastDay, AgriWeatherForecast, AgriculturalInsights, AgriWeatherDa |
| `Alert` | `src/modules/farm_manager/models/alert.py:58` | alert.py - Cycle/block/farm level alert model with severity, status, type, escalation and resolution. Persists to the alerts collection. | AlertSeverity, AlertStatus, AlertType, AlertCreate, AlertResolve, Alert, AlertLi |
| `AlertCategory` | `src/modules/farm_manager/models/block_alert.py:32` | Enum (block_alert.py): alert category (pest, disease, irrigation, equipment, environmental, other) used for filtering and analytics. | AlertCategory |
| `AlertComment` | `src/modules/farm_manager/models/block_alert.py:60` | Embedded threaded comment on a BlockAlert (author, body, timestamp). | AlertComment |
| `AlertSeverity` | `src/modules/farm_manager/models/alert.py:14` | Enum (alert.py): info, warning, critical. Reused by block_cycle.py's BlockCycleAlert. Distinct from block_alert.py's AlertSeverity. | AlertSeverity |
| `AlertSeverity` | `src/modules/farm_manager/models/block_alert.py:14` | Enum (block_alert.py): severity scale for per-block alerts. Distinct from alert.py's AlertSeverity - both names exist in this module. | AlertSeverity |
| `AlertStatus` | `src/modules/farm_manager/models/alert.py:23` | Enum (alert.py): active, acknowledged, resolved. Distinct from block_alert.py's AlertStatus. | AlertStatus |
| `AlertStatus` | `src/modules/farm_manager/models/block_alert.py:23` | Enum (block_alert.py): status lifecycle for per-block alerts. | AlertStatus |
| `AlertType` | `src/modules/farm_manager/models/alert.py:31` | Enum (alert.py): the alert trigger class - overdue, yield_variance, environmental, pest_disease and related. | AlertType |
| `AlertsSummary` | `src/modules/farm_manager/models/block_archive.py:22` | Embedded alert counts (total, by severity, resolved) on BlockArchive. | AlertsSummary |
| `AssetCategory` | `src/modules/farm_manager/models/inventory.py:240` | Enum: tractor, harvester, irrigation_system, greenhouse, storage_facility, vehicle, tool, equipment, infrastructure, sensor, other. | AssetCategory |
| `AssetInventory` | `src/modules/farm_manager/models/inventory.py:819` | Farm equipment/asset record with AssetCategory, AssetStatus, current allocation and maintenance schedule/overdue flag. Persists to the inventory_asset collection. | AssetInventoryBase, AssetInventoryCreate, AssetInventoryUpdate, AssetInventory |
| `AssetStatus` | `src/modules/farm_manager/models/inventory.py:256` | Enum: operational, maintenance, repair, decommissioned, stored. | AssetStatus |
| `BaseUnit` | `src/modules/farm_manager/models/inventory.py:71` | Enum: the canonical storage unit an input quantity is normalized to (mg / ml / unit family). Together with MASS_TO_MG, VOLUME_TO_ML and COUNT_TO_UNIT it makes quantities comparable regardless of the unit the user entered. | BaseUnit |
| `Block` | `src/modules/farm_manager/models/block.py:341` | block.py - Block document model: status lifecycle (BlockStatus), physical vs virtual blocks, IoT/SenseHub controller, KPI + historical KPI, plant-data snapshot, multi-crop virtual planting. targetCrop stays the VARIETY (plant_data_enhanced); Plant Library Phase 1 added productMotherId + productName (line 409) carrying the MOTHER product/SKU, stamped at planting and indexed. Persists to the blocks collection. | BlockStatus, BlockType, BlockLocation, SenseHubCredentials, IoTController, Block |
| `BlockAlert` | `src/modules/farm_manager/models/block_alert.py:83` | block_alert.py - Per-block alert model with severity, status, category and threaded AlertComment list. Note: defines its own AlertSeverity/AlertStatus, distinct from alert.py's. Persists to the block_alerts collection. | AlertSeverity, AlertStatus, AlertCategory, BlockAlertCreate, AlertComment, Block |
| `BlockAnalyticsResponse` | `src/modules/farm_manager/models/block_analytics.py:298` | block_analytics.py - Read-only per-block analytics response models (no collection of their own): yield trend, state timeline, task stats, performance metrics and alert analytics, aggregated over block_harvests, farm_tasks and block_alerts. | TimePeriod, TrendDirection, StateProgressStep, BlockInfoAnalytics, YieldTrendPoi |
| `BlockArchive` | `src/modules/farm_manager/models/block_archive.py:32` | block_archive.py - Archive record for a completed block cycle: yield totals, QualityBreakdown, AlertsSummary, farming years planted/harvested. Carries productMotherId + productName (line 66, Plant Library Phase 1, optional - pre-migration archives leave them unset) so archived cycles roll up to the same product as the live block did. Persists to the block_archives collection. | QualityBreakdown, AlertsSummary, BlockArchive, BlockArchiveListResponse, BlockAr |
| `BlockCycle` | `src/modules/farm_manager/models/block_cycle.py:36` | block_cycle.py - One complete planting-to-harvest sequence for a block, with embedded alert and daily-harvest summaries. The primary source for historical analytics and AI training. Persists to the block_cycles collection. | BlockCycleAlert, BlockCycleDailyHarvest, BlockCycle |
| `BlockHarvest` | `src/modules/farm_manager/models/block_harvest.py:81` | block_harvest.py - Harvest event model for the SELLABLE ledger. Stage 3 of the Plant Library product extension added optional productId, productName (FROZEN name snapshot, never re-synced) and harvestBatchId to BlockHarvest, plus the multi-line batch submission models (HarvestBatchLineCreate/SubmitRequest/LineResult/SubmitResponse, lines 187-247) and the batch-lookup models (HarvestBatchLookupLine/Group/LookupResponse, lines 250-276) that union the three destinations. Persists to the block_harvests collection. | QualityGrade, BlockHarvestBase, BlockHarvestCreate, BlockHarvestUpdate, HarvestM |
| `BlockHarvestSummary` | `src/modules/farm_manager/models/block_harvest.py:163` | Per-block harvest rollup: total harvests, total kg, per-grade kg (A/B/C), average grade, first/last harvest date. Computed, not stored. | BlockHarvestSummary |
| `BlockHistoryArchive` | `src/modules/farm_manager/models/block_history.py:16` | block_history.py - Complete snapshot of a block's lifecycle from planting to completion, written on the CLEANING -> EMPTY transition. Reuses BlockStatus/BlockType/BlockLocation/BlockKPI/StatusChange from block.py. Persists to the block_history collection. | BlockHistoryArchive, BlockHistoryCreate, BlockHistoryListResponse, BlockHistoryR |
| `BlockKPI` | `src/modules/farm_manager/models/block.py:169` | Embedded KPI block on Block: expected vs actual yield, yield variance, cycle duration and a PerformanceCategory rating. | BlockKPI |
| `BlockStatus` | `src/modules/farm_manager/models/block.py:17` | Enum: the block state machine - empty, planting, growing, harvesting, cleaning, maintenance, inactive. Drives task generation and dashboard blocks-by-state counts. | BlockStatus |
| `BlockType` | `src/modules/farm_manager/models/block.py:32` | Enum: block type - greenhouse, open_field, hydroponic, nursery, vertical_farm, virtual (a virtual crop inside a physical block). | BlockType |
| `CalculationList` | `src/modules/farm_manager/models/tools/calculation_list.py:32` | tools/calculation_list.py - A saved fertilizer calculation list (crop/area line items) scoped to an organization. Persists to the fertilizer_calculation_lists collection. | CalculationListItem, CalculationList, CalculationListCreate, CalculationListUpda |
| `CurrentWeather` | `src/modules/farm_manager/models/weather.py:118` | Current conditions for a farm location, embedding SolarData, AirQuality and SoilConditions. Response-only. | SolarData, AirQuality, SoilConditions, CurrentWeather |
| `DailyHarvest` | `src/modules/farm_manager/models/daily_harvest.py:41` | daily_harvest.py - Individual daily harvest record for a cycle/planting/block, with per-grade DailyHarvestEntry lines. Persists to the daily_harvests collection. | DailyHarvestEntry, DailyHarvestCreate, DailyHarvest |
| `DashboardSummary` | `src/modules/farm_manager/models/dashboard.py:139` | dashboard.py - Read-only dashboard response models (no collection of their own): block/farm summaries, blocks-by-state counts, harvest rollups, yield KPIs, crop breakdown and farming-year context. Composed from blocks, block_harvests, block_alerts and farms at query time. | BlockCalculated, DashboardAlert, DashboardBlock, FarmInfo, DashboardSummary, Das |
| `Deleted archive models` | `src/modules/farm_manager/models/deleted_archives.py:15` | deleted_archives.py - Tombstone models written by the cascade deletion service. Deleting a farm or block moves its documents into parallel deleted_* collections so the live collections stay clean for AI analytics while history is preserved. Four models, four collections. | DeletedFarm, DeletedBlock, DeletedBlockArchive, DeletedBlockHarvest |
| `DeletedBlock` | `src/modules/farm_manager/models/deleted_archives.py:59` | Tombstone copy of a deleted block. Persists to the deleted_blocks collection. | DeletedBlock |
| `DeletedBlockArchive` | `src/modules/farm_manager/models/deleted_archives.py:117` | Tombstone copy of a deleted block archive. Persists to the deleted_block_archives collection. | DeletedBlockArchive |
| `DeletedBlockHarvest` | `src/modules/farm_manager/models/deleted_archives.py:175` | Tombstone copy of a deleted harvest row. Persists to the deleted_block_harvests collection. | DeletedBlockHarvest |
| `DisplayUnit` | `src/modules/farm_manager/models/inventory.py:85` | Enum: the unit a normalized base quantity is rendered back into for display (kg, g, L, ml, unit...). | DisplayUnit |
| `DisposalMethod` | `src/modules/farm_manager/models/inventory.py:1163` | Enum: compost, animal_feed, discard, sold_discount, donated, pending. | DisposalMethod |
| `Farm` | `src/modules/farm_manager/models/farm.py:89` | farm.py - Farm document model (farmId, location, GeoJSON boundary, manager, metadata) plus its create/update schemas. Persists to the farms collection. | FarmLocation, GeoJSONPolygon, FarmBoundary, FarmBase, FarmCreate, FarmUpdate, Fa |
| `FarmAnalyticsResponse` | `src/modules/farm_manager/models/farm_analytics.py:124` | farm_analytics.py - Read-only farm-level analytics response models (no collection of their own): aggregated metrics, block state breakdown, block comparison, yield timeline and historical trends. | AggregatedMetrics, StateBreakdownItem, StateBreakdown, BlockComparisonItem, Yiel |
| `FarmAssignment` | `src/modules/farm_manager/models/farm_assignment.py:22` | farm_assignment.py - Maps a user to a farm for access control (assignmentId, userId, farmId, isActive). Persists to the farm_assignments collection. | FarmAssignmentCreate, FarmAssignment |
| `FarmTask` | `src/modules/farm_manager/models/farm_task.py:180` | Complete farm task document stored in the farm_tasks collection. Includes taskData (harvestEntries, totalHarvest, notes, photoUrls), status, completion tracking, divisionId/organizationId scoping. | FarmTask |
| `FarmTask` | `src/modules/farm_manager/models/farm_task.py:192` | farm_task.py - Operations task model: type, priority, status, assignment, and taskData carrying harvest entries + rolled-up totals. FarmTaskWithDetails adds joined block/farm context for the task UI. Persists to the farm_tasks collection. Per-class nodes for this file live under the farm_manager.models.farm_task.* namespace. | TaskType, TaskStatus, TaskPriority, HarvestGrade, HarvestEntry, HarvestTotal, Ta |
| `FarmTaskCreate` | `src/modules/farm_manager/models/farm_task.py:160` | Request schema for creating a new farm task (farmId, blockId, taskType, scheduledDate, priority, assignedTo, triggerStateChange). | FarmTaskCreate |
| `FarmTaskListResponse` | `src/modules/farm_manager/models/farm_task.py:288` | Paginated list response wrapper for FarmTask. | FarmTaskListResponse |
| `FarmTaskUpdate` | `src/modules/farm_manager/models/farm_task.py:279` | Partial-update schema for tasks (scheduledDate, dueDate, status, priority, description). | FarmTaskUpdate |
| `FarmTaskWithDetails` | `src/modules/farm_manager/models/farm_task.py:257` | v1.11.0 NEW: FarmTask extended with joined block/farm context. Optional fields: blockCode, blockName, targetCrop, targetCropName, actualPlantCount, expectedYieldKg (from blocks); farmCode, farmName (from farms). Used by Operations task UI and harvest-entry modals so clients don't need follow-up fetches. | FarmTaskWithDetails |
| `FarmTypeEnum` | `src/modules/farm_manager/models/plant_data_enhanced.py:23` | Enum: which farm types a variety can be grown in (greenhouse, open_field, hydroponic, vertical...). Drives plant-library filtering by block type. | FarmTypeEnum |
| `FarmingYearConfig` | `src/modules/farm_manager/models/farming_year_config.py:18` | farming_year_config.py - Per-organization farming year definition (default start month = August) plus the get_farming_year / get_farming_year_date_range helpers used to stamp farmingYear on harvests and archives. Stored as a singleton document in the system_config collection. | DEFAULT_FARMING_YEAR_START_MONTH, FarmingYearConfig, FarmingYearConfigUpdate, Fa |
| `FertigationRuleTypeEnum` | `src/modules/farm_manager/models/plant_data_enhanced.py:384` | Enum: how a FertigationRule is applied (per_stage, per_day, custom). | FertigationRuleTypeEnum |
| `FertigationSchedule` | `src/modules/farm_manager/models/plant_data_enhanced.py:483` | The variety's full fertigation programme: FertigationCard groups of FertigationRule entries over FertigationIngredient lines, plus CustomApplication overrides. | FertigationIngredient, CustomApplication, FertigationRule, FertigationCard, Fert |
| `Fertilizer calculator DTOs` | `src/modules/farm_manager/models/tools/calculator_request.py:32` | tools/calculator_request.py - Request/response DTOs for the Fertilizer Cost Calculator (no collection of their own): the calculate request lines, per-ingredient and per-crop results, and the spreadsheet-import parse result with skipped rows. | CalculateItem, CalculateRequest, IngredientResult, CropResult, CalculateResponse |
| `FertilizerChemical` | `src/modules/farm_manager/models/tools/fertilizer_chemical.py:18` | tools/fertilizer_chemical.py - Master chemical catalog for the Fertilizer Cost Calculator: canonical name, aliases, IngredientCategoryEnum category and default pricing unit (kg or L). Soft-deleted via archivedAt. Persists to the fertilizer_chemicals collection. | FertilizerChemical, ChemicalCreate, ChemicalUpdate |
| `GlobalAnalyticsResponse` | `src/modules/farm_manager/models/global_analytics.py:105` | global_analytics.py - Read-only cross-farm analytics response models (no collection of their own): per-farm summaries, global aggregated metrics, state breakdown, yield timeline and performance insights. | FarmSummaryItem, GlobalAggregatedMetrics, GlobalStateBreakdown, GlobalYieldTimel |
| `GrowthCycleDuration` | `src/modules/farm_manager/models/plant_data_enhanced.py:115` | Per-stage day counts for a variety (germination through harvest) used to project estimatedHarvestDate on a block. | GrowthCycleDuration |
| `GrowthHabitEnum` | `src/modules/farm_manager/models/plant_data_enhanced.py:90` | Enum: determinate, indeterminate, bush, vine and related growth habits. | GrowthHabitEnum |
| `GrowthStageEnum` | `src/modules/farm_manager/models/plant_data_enhanced.py:33` | Enum: germination, seedling, vegetative, flowering, fruiting, harvest - the stage axis for fertigation rules and growth cycle duration. | GrowthStageEnum |
| `Harvest` | `src/modules/farm_manager/models/harvest.py:26` | harvest.py - Final aggregated harvest summary rolled up from daily harvests. Legacy model kept for backward compatibility and reporting. Persists to the harvests collection. | HarvestEntry, Harvest |
| `HarvestBatchLookupResponse` | `src/modules/farm_manager/models/block_harvest.py:271` | Response for GET .../harvests/batch-lookup: given a block + harvest date, unions all three destinations into HarvestBatchGroup/HarvestBatchLookupLine so a mixed submission can be reviewed as a unit. The default harvest list stays block_harvests-only. | HarvestBatchLookupLine, HarvestBatchGroup, HarvestBatchLookupResponse |
| `HarvestBatchSubmitRequest` | `src/modules/farm_manager/models/block_harvest.py:207` | Request body for POST .../harvests/batch: one harvestDate plus >=1 HarvestBatchLineCreate product lines. The server generates one shared harvestBatchId and routes each line by its product category. | HarvestBatchLineCreate, HarvestBatchSubmitRequest |
| `HarvestBatchSubmitResponse` | `src/modules/farm_manager/models/block_harvest.py:233` | Response for a completed multi-line harvest submission: the harvestBatchId plus a HarvestBatchLineResult per line recording category, destination collection (block_harvests | processing_inventory | inventory_waste) and the created recordId. | HarvestBatchLineResult, HarvestBatchSubmitResponse |
| `HarvestEntry` | `src/modules/farm_manager/models/farm_task.py:49` | Single harvest entry attached to a daily_harvest task (entryId, userId, userEmail, timestamp, quantity, grade, notes). | HarvestEntry |
| `HarvestEntryCreate` | `src/modules/farm_manager/models/farm_task.py:243` | Request schema for adding a harvest entry (quantity > 0, grade, optional notes). | HarvestEntryCreate |
| `HarvestGrade` | `src/modules/farm_manager/models/farm_task.py:40` | Enum: A, B, C, D, Waste. | HarvestGrade |
| `HarvestInventory` | `src/modules/farm_manager/models/inventory.py:475` | Sellable harvest stock item: quality grade, quantities with reservation/available split for FIFO order fulfilment, expiry and source block/plant links. Persists to the inventory_harvest collection. | HarvestInventoryBase, HarvestInventoryCreate, HarvestInventoryUpdate, HarvestInv |
| `HarvestMetadata` | `src/modules/farm_manager/models/block_harvest.py:58` | Embedded free-form metadata on BlockHarvest carrying migration provenance (migratedFrom/At, oldRef, legacyBlockCode, season). Config.extra = allow. | HarvestMetadata |
| `HarvestProductType` | `src/modules/farm_manager/models/inventory.py:44` | Enum: fresh, processed, dried, frozen, packaged - the form a harvest inventory item is held in. | HarvestProductType |
| `HarvestTotal` | `src/modules/farm_manager/models/farm_task.py:73` | Aggregated harvest totals (totalQuantity, gradeBreakdown per grade, contributors, entryCount). | HarvestTotal |
| `HistoricalKPI` | `src/modules/farm_manager/models/block.py:216` | Embedded on physical Blocks only: KPIs aggregated across completed virtual-block cycles. | HistoricalKPI |
| `IngredientCategoryEnum` | `src/modules/farm_manager/models/plant_data_enhanced.py:373` | Enum: fertigation ingredient class (macro, micro, acid, base, biological...). Also imported by tools/fertilizer_chemical.py so the calculator's chemical catalog shares one vocabulary with the plant library. | IngredientCategoryEnum |
| `InputCategory` | `src/modules/farm_manager/models/inventory.py:54` | Enum: fertilizer, pesticide, herbicide, fungicide, seed, seedling, soil, substrate and related input classes. Each category maps to a BaseUnit via CATEGORY_BASE_UNIT. | InputCategory |
| `InputInventory` | `src/modules/farm_manager/models/inventory.py:645` | Farm input supply item (fertilizer, pesticide, seed, soil...) with InputCategory, base-unit normalized quantity and low-stock flagging. Persists to the inventory_input collection. | InputInventoryBase, InputInventoryCreate, InputInventoryUpdate, InputInventory |
| `Inventory models` | `src/modules/farm_manager/models/inventory.py:1` | inventory.py - The farm inventory model file (1528 lines). Holds EIGHT separately persisted document models, each with its own collection: Product (products), HarvestInventory (inventory_harvest), InputInventory (inventory_input), AssetInventory (inventory_asset), InventoryMovement (inventory_movements), ProcessingInventory (processing_inventory, NEW), WasteInventory (inventory_waste) and ReturnedInventory (inventory_returned) - see the farm_manager.models.inventory.* nodes. Also holds the unit-conversion tables (MASS_TO_MG, VOLUME_TO_ML, COUNT_TO_UNIT, CATEGORY_BASE_UNIT) and helpers that normalize input quantities to a base unit. | InventoryScope, InventoryType, HarvestProductType, InputCategory, BaseUnit, Disp |
| `InventoryMovement` | `src/modules/farm_manager/models/inventory.py:882` | Append-only movement ledger entry recording every quantity change against any inventory type, typed by MovementType (addition, removal, adjustment, transfer, sale, waste, usage, return, restoration, reservation, shipment). Persists to the inventory_movements collection. | InventoryMovement |
| `InventoryScope` | `src/modules/farm_manager/models/inventory.py:29` | Enum: organization (default inventory, farmId null) | farm (farm-specific). Indexed on all three inventory collections. | InventoryScope |
| `InventoryType` | `src/modules/farm_manager/models/inventory.py:36` | Enum: harvest | input | asset - the three main inventory categories, each with its own collection. | InventoryType |
| `IoTController` | `src/modules/farm_manager/models/block.py:61` | Embedded IoT/SenseHub controller config on Block, including SenseHubCredentials and last-sync/staleness fields (surfaced by BlockWithStaleness). | IoTController |
| `LightTypeEnum` | `src/modules/farm_manager/models/plant_data_enhanced.py:60` | Enum: natural, artificial, supplemental light source for LightRequirements. | LightTypeEnum |
| `MovementType` | `src/modules/farm_manager/models/inventory.py:277` | Enum: addition, removal, adjustment, transfer, sale, waste, usage, return, restoration (reviving an expired batch), reservation (stock held for a confirmed order), shipment (stock leaves on ship). | MovementType |
| `PerformanceCategory` | `src/modules/farm_manager/models/block.py:158` | Enum: KPI performance banding used by BlockKPI - excellent, good, average, poor. | PerformanceCategory |
| `PlantData` | `src/modules/farm_manager/models/plant_data.py:129` | plant_data.py - Simple/legacy plant catalog model with DataContributor attribution. Superseded by plant_data_enhanced.py for new work. Persists to the plant_data collection. | DataContributor, PlantDataBase, PlantDataCreate, PlantDataUpdate, PlantData |
| `PlantDataEnhanced` | `src/modules/farm_manager/models/plant_data_enhanced.py:639` | plant_data_enhanced.py - The VARIETY level of the two-level Plant Library: the full cultivation recipe (growth cycle, yield, environment, watering, soil, light, spacing, economics, fertigation cards/rules/schedules). Variety-aware since Phase 1: motherPlantId + varietyName (lines 631-672) link each row to its PlantMother. Persists to the plant_data_enhanced collection. | FarmTypeEnum, GrowthStageEnum, ToleranceLevelEnum, SeverityLevelEnum, LightTypeE |
| `PlantDataSnapshot` | `src/modules/farm_manager/models/block.py:233` | Embedded frozen copy of the planted variety's cultivation data, taken at planting so a later plant_data_enhanced edit does not rewrite a running cycle. | PlantDataSnapshot |
| `PlantMother` | `src/modules/farm_manager/models/plant_mother.py:184` | plant_mother.py - The MOTHER (product/SKU) level of the Plant Library; harvest, inventory and sales roll up here rather than to individual varieties. Product extension Stage 1 added the per-mother products[] picklist (PlantProduct, line 115) with routing category sellable | process | waste, embedded rather than a separate collection because products are meaningless outside their mother. Also carries the Phase 2 composite response shapes. Persists to the plant_mothers collection. | ProductUnit, ProductCategory, PlantProductBase, PlantProductCreate, PlantProduct |
| `PlantMotherWithVarieties` | `src/modules/farm_manager/models/plant_mother.py:278` | Composite response for GET /plant-mothers/{id}: PlantMother plus its active varieties as lightweight VarietySummary entries. PlantMotherWithVarietyCount is the list-endpoint counterpart (varietyCount computed by the repository, not stored). | PlantMotherWithVarietyCount, VarietySummary, PlantMotherWithVarieties |
| `PlantProduct` | `src/modules/farm_manager/models/plant_mother.py:115` | A concrete product a mother yields (e.g. Capsicum -> Green Capsicum / Capsicum Puree / Capsicum Trim). EMBEDDED in PlantMother.products[], not its own collection. Stable productId (UUID, never reused), name unique within the mother, ProductUnit, ProductCategory, isActive. Deletion is deactivation only - never removed from the array - so harvest rows referencing productId need no migration. | PlantProductBase, PlantProductCreate, PlantProductUpdate, PlantProduct |
| `Planting` | `src/modules/farm_manager/models/planting.py:39` | planting.py - Planting plan assigning one or more PlantingItem crop lines to a block. Persists to the plantings collection. | PlantingItem, PlantingBase, PlantingCreate, Planting |
| `PriceOverride` | `src/modules/farm_manager/models/tools/fertilizer_price.py:15` | tools/fertilizer_price.py - Per-organization price override for a chemical, plus the ResolvedPrice value object returned after applying the price book. Persists to the fertilizer_price_overrides collection (unique on chemicalId + organizationId). | PriceOverride, ResolvedPrice |
| `ProcessingInventory` | `src/modules/farm_manager/models/inventory.py:1117` | NEW (Plant Library product extension Stage 3, design doc §4.4). Destination record for a `process`-category harvest line: productId + FROZEN productName snapshot, quantity/unit, an A/B/C qualityGrade (process output IS graded, unlike waste), harvestDate and the shared harvestBatchId. Deliberately kept out of inventory_harvest, which stays sellable-only, and given no reservation/availability/FIFO fields (out of scope until process output is sold directly). Persists to the processing_inventory collection. | ProcessingInventoryBase, ProcessingInventoryCreate, ProcessingInventory |
| `Product` | `src/modules/farm_manager/models/inventory.py:298` | Master product catalog entry, organization-wide. Persists to the products collection. NOT the same thing as PlantMother.products[] / PlantProduct, which is the embedded Plant Library picklist. | Product, ProductCreate, ProductUpdate |
| `ProductCategory` | `src/modules/farm_manager/models/plant_mother.py:62` | Enum: sellable | process | waste. Fixed vocabulary, no user-created categories. Governs harvest-line routing: sellable -> block_harvests, process -> processing_inventory, waste -> inventory_waste. Sellable is the ONLY category that may ever produce a block_harvests row. | ProductCategory |
| `ProductUnit` | `src/modules/farm_manager/models/plant_mother.py:50` | Enum: unit of measure for a PlantProduct. kg is the only member today - deliberately an enum so a future animal-husbandry module can add head/litre without backfilling existing harvest rows. | ProductUnit |
| `QualityBreakdown` | `src/modules/farm_manager/models/block_archive.py:14` | Embedded per-grade kg breakdown on BlockArchive. | QualityBreakdown |
| `QualityGrade` | `src/modules/farm_manager/models/block_harvest.py:23` | Enum: A | B | C harvest quality scale. The canonical harvest grade - inventory.py imports it as HarvestQualityGrade for ProcessingInventory so one grade picklist works for both sellable and process lines. Distinct from inventory.py's own six-value QualityGrade. | QualityGrade |
| `QualityGrade` | `src/modules/farm_manager/models/inventory.py:266` | Enum: premium, grade_a, grade_b, grade_c, processing, rejected - the SIX-value inventory grade scale. Distinct from block_harvest.py's three-value A/B/C QualityGrade, which is what harvest and processing lines use. | QualityGrade |
| `ReturnedInventory` | `src/modules/farm_manager/models/inventory.py:1476` | Goods returned by a customer (customer-origin, unlike farm-origin HarvestInventory), tracked for re-stock or write-off. Persists to the inventory_returned collection. | ReturnedInventoryBase, ReturnedInventoryCreate, ReturnedInventoryUpdate, Returne |
| `SeverityLevelEnum` | `src/modules/farm_manager/models/plant_data_enhanced.py:51` | Enum: severity rating for a DiseaseOrPest entry. | SeverityLevelEnum |
| `SoilTypeEnum` | `src/modules/farm_manager/models/plant_data_enhanced.py:79` | Enum: soil/substrate class for SoilRequirements. | SoilTypeEnum |
| `SpacingCategory` | `src/modules/farm_manager/models/spacing_standards.py:15` | Enum: the plant spacing band (very_dense through very_sparse) that maps to a plants-per-100sqm density. Imported by both plant_data.py and plant_data_enhanced.py. | SpacingCategory |
| `SpacingRequirements` | `src/modules/farm_manager/models/plant_data_enhanced.py:336` | Per-variety plant spacing and plants-per-100sqm, resolved against the shared SpacingCategory standards by resolve_default_plants_per_100m2. | SpacingRequirements |
| `SpacingStandardsConfig` | `src/modules/farm_manager/models/spacing_standards.py:63` | spacing_standards.py - Plant spacing standards: per-SpacingCategory densities (plants per 100 sqm) plus the plant-count / area conversion helpers used by block planting and plant_data. Stored as a singleton document in the system_config collection. | SpacingCategory, SpacingDensity, SpacingStandardsConfig, SpacingStandardsUpdate, |
| `StatusChange` | `src/modules/farm_manager/models/block.py:122` | Embedded status-transition record appended to Block.statusHistory; reused by block_archive.py and block_history.py. | StatusChange |
| `StockInventoryItem` | `src/modules/farm_manager/models/stock_inventory.py:32` | stock_inventory.py - FIFO stock/harvest inventory line keyed by farm, plant, block, cycle and daily harvest with a quality grade. Persists to the stock_inventory collection. | StockInventoryCreate, StockInventoryItem |
| `SupportTypeEnum` | `src/modules/farm_manager/models/plant_data_enhanced.py:101` | Enum: trellis, stake, cage, none - the physical support a variety needs. | SupportTypeEnum |
| `TaskCompletionData` | `src/modules/farm_manager/models/farm_task.py:250` | Request schema for completing a non-harvest task (notes, photoUrls, triggerTransition for Phase 2 block state change). | TaskCompletionData |
| `TaskData` | `src/modules/farm_manager/models/farm_task.py:106` | Task-specific completion data: harvestEntries list, totalHarvest, notes, photoUrls. Validators coerce None to empty list for backward compatibility. | TaskData |
| `TaskPriority` | `src/modules/farm_manager/models/farm_task.py:33` | Enum: high, medium, low. | TaskPriority |
| `TaskStatus` | `src/modules/farm_manager/models/farm_task.py:25` | Enum: pending, in_progress, completed, cancelled. | TaskStatus |
| `TaskType` | `src/modules/farm_manager/models/farm_task.py:14` | Enum: planting, fruiting_check, harvest_readiness, daily_harvest, harvest_completion, cleaning, custom. | TaskType |
| `TimePeriod` | `src/modules/farm_manager/models/block_analytics.py:15` | Enum: the analytics rollup window (week, month, quarter, year, all). | TimePeriod |
| `ToleranceLevelEnum` | `src/modules/farm_manager/models/plant_data_enhanced.py:43` | Enum: low | medium | high tolerance rating used across environmental requirements. | ToleranceLevelEnum |
| `TransferRecord` | `src/modules/farm_manager/models/inventory.py:1045` | Record of an inventory transfer between farms/scopes, produced by a TransferRequest and mirrored into inventory_movements as MovementType.TRANSFER. | TransferRequest, TransferResponse, TransferRecord |
| `TrendDirection` | `src/modules/farm_manager/models/block_analytics.py:25` | Enum: up | down | stable, used to annotate analytics trends. | TrendDirection |
| `VarietyCreateForMother` | `src/modules/farm_manager/models/plant_mother.py:289` | Request body for POST /plant-mothers/{motherPlantId}/varieties. Extends PlantDataEnhancedBase; plantName/scientificName are inherited from the mother and ignored, varietyName is the one new required field (unique per mother). | VarietyCreateForMother |
| `WasteInventory` | `src/modules/farm_manager/models/inventory.py:1259` | Waste ledger entry with WasteSourceType provenance (harvest, return, expired, damaged, quality_reject, order_deletion), DisposalMethod, estimated value and source links. Stage 3 added optional productId + harvestBatchId, populated only for waste lines routed straight here by the multi-line harvest submission; the pre-existing write paths leave both null. Harvest waste is NOT graded - a waste line supplying a qualityGrade is rejected 422. Persists to the inventory_waste collection. | WasteInventoryBase, WasteInventoryCreate, WasteInventoryUpdate, WasteInventory |
| `WasteSourceType` | `src/modules/farm_manager/models/inventory.py:1151` | Enum: harvest, return, expired, damaged, quality_reject, other, order_deletion. Harvest-sourced waste is what the Stage 3 batch routing writes. | WasteSourceType |
| `WaterTypeEnum` | `src/modules/farm_manager/models/plant_data_enhanced.py:69` | Enum: water source/quality class for WateringRequirements. | WaterTypeEnum |
| `Weather models` | `src/modules/farm_manager/models/weather.py:1` | weather.py - WeatherBit-derived models: current conditions (with solar, air quality and soil sub-models), agricultural forecast days, agronomic insights, and the WeatherCacheEntry envelope that is the only persisted model here (weather_cache collection). | SolarData, AirQuality, SoilConditions, CurrentWeather, AgriWeatherForecastDay, A |
| `WeatherCacheEntry` | `src/modules/farm_manager/models/weather.py:317` | Cache envelope wrapping a WeatherBit response with its location key and TTL - the only persisted model in weather.py. Persists to the weather_cache collection. | WeatherCacheEntry |
| `YieldInfo` | `src/modules/farm_manager/models/plant_data_enhanced.py:151` | Expected yield per plant / per sqm and waste percentage for a variety - the basis for a block's expected yield KPI. | YieldInfo |
| `ai_dashboard_reports` | `src/modules/farm_manager/services/ai_dashboard/service.py:19` | MongoDB collection: ai_dashboard_reports - persisted AI Dashboard inspection reports (raw inspection data + Gemini-generated AISummary), written every 4 hours by AIDashboardScheduler. |
| `ai_hub_chat_log` | `src/modules/farm_manager/services/ai_hub/service.py` | MongoDB collection: ai_hub_chat_log - conversation log for the AI Hub assistant (farm_manager/services/ai_hub/service.py writes on every turn and replays history from it). Fields: section, userId, userMessage, assistantMessage, toolsUsed, hasPendingAction, timestamp. LIVE: 43 docs, indexed on (section, userId, timestamp). |
| `alerts` | `src/modules/farm_manager/services/database.py` | MongoDB collection: alerts - cycle/block/farm level alerts with severity, status, escalation. Persists models/alert.py Alert. |
| `block_alerts` | `src/modules/farm_manager/services/block/alert_repository.py` | MongoDB collection: block_alerts - per-block alerts with severity/status/category and threaded comments. Persists models/block_alert.py BlockAlert. |
| `block_archives` | `src/modules/farm_manager/services/database.py` | MongoDB collection: block_archives - completed growing cycles archived at cleaning->empty. Persists models/block_archive.py BlockArchive, including the Phase 1 productMotherId/productName mother (product/SKU) rollup copied from the block. |
| `block_cycles` | `src/modules/farm_manager/services/database.py` | MongoDB collection: block_cycles - one planting-to-harvest sequence per block; source for historical analytics/AI training. Persists models/block_cycle.py BlockCycle. |
| `block_harvests` | `src/modules/farm_manager/services/database.py` | MongoDB collection: block_harvests - the SELLABLE harvest ledger ONLY (Plant Library product extension Stage 3, design doc §3.1: process/waste lines must never become rows here). Persists models/block_harvest.py BlockHarvest. Stage 3 added optional productId, productName (FROZEN snapshot, never re-synced on rename) and harvestBatchId, all indexed; ~13,947 legacy rows keep nulls and are not backfilled. |
| `block_history` | `src/modules/farm_manager/services/block_history/block_history_repository.py` | MongoDB collection: block_history - full block lifecycle snapshots written on the cleaning->empty transition. Persists models/block_history.py BlockHistoryArchive. |
| `blocks` | `src/modules/farm_manager/services/database.py` | MongoDB collection: blocks - physical and virtual growing blocks with the status state machine. Persists models/block.py Block. Indexed on blockId (unique), farmId, state, currentPlanting, currentCycleId, estimatedHarvestDate and (Plant Library Phase 1) productMotherId. |
| `daily_harvests` | `src/modules/farm_manager/services/database.py` | MongoDB collection: daily_harvests - individual daily harvest entries per cycle/planting/block. Persists models/daily_harvest.py DailyHarvest. |
| `deleted_block_archives` | `src/modules/farm_manager/services/cascade_deletion_service.py` | MongoDB collection: deleted_block_archives - block archives preserved on cascade delete. Persists models/deleted_archives.py DeletedBlockArchive. |
| `deleted_block_harvests` | `src/modules/farm_manager/services/cascade_deletion_service.py` | MongoDB collection: deleted_block_harvests - harvest rows preserved on cascade delete. Persists models/deleted_archives.py DeletedBlockHarvest. |
| `deleted_blocks` | `src/modules/farm_manager/services/cascade_deletion_service.py` | MongoDB collection: deleted_blocks - blocks preserved on cascade delete. Persists models/deleted_archives.py DeletedBlock. |
| `deleted_farms` | `src/modules/farm_manager/services/cascade_deletion_service.py` | MongoDB collection: deleted_farms - farms moved here on delete so the live collections stay clean for AI analytics. Persists models/deleted_archives.py DeletedFarm. |
| `farm_ai_chat_log` | `src/modules/farm_manager/services/farm_ai/service.py` | MongoDB collection: farm_ai_chat_log - shared conversation log for the three scoped farm assistants: farm_ai (block-level), farm_level_ai and global_ai all insert into it. Fields: farmId, blockId, userId, userMessage, assistantMessage, toolsUsed, hasPendingAction, timestamp. LIVE: 74 docs, NO indexes beyond _id despite always being queried by farmId/blockId + timestamp. |
| `farm_assignments` | `src/modules/farm_manager/services/database.py` | MongoDB collection: farm_assignments - maps users to farms for access control. Persists models/farm_assignment.py FarmAssignment. |
| `farm_tasks` | `src/modules/farm_manager/services/task/task_repository.py` | MongoDB collection: farm_tasks - operations task manager for farming activities. Persists models/farm_task.py FarmTask (taskData.harvestEntries, totalHarvest, status, division/organization scoping). |
| `farmer_assignments` | `src/modules/farm_manager/services/task/task_service.py` | MongoDB collection: farmer_assignments - assigns farmers/workers to specific farms |
| `farms` | `src/modules/farm_manager/services/farm/farm_repository.py` | MongoDB collection: farms - farm documents (farmId unique, managerId, isActive). Persists models/farm.py Farm. |
| `fertilizer_calculation_lists` | `src/modules/farm_manager/services/tools/calculation_lists_repository.py` | MongoDB collection: fertilizer_calculation_lists - saved fertilizer calculation lists per organization. Persists models/tools/calculation_list.py CalculationList. |
| `fertilizer_chemicals` | `src/modules/farm_manager/services/tools/chemicals_repository.py` | MongoDB collection: fertilizer_chemicals - master chemical catalog for the Fertilizer Cost Calculator (canonical name, aliases, category, pricing unit; soft-deleted via archivedAt). Persists models/tools/fertilizer_chemical.py FertilizerChemical. |
| `fertilizer_price_overrides` | `src/modules/farm_manager/services/tools/price_book.py` | MongoDB collection: fertilizer_price_overrides - per-organization price per chemical (unique on chemicalId+organizationId). Persists models/tools/fertilizer_price.py PriceOverride. |
| `harvests` | `src/modules/farm_manager/services/database.py` | MongoDB collection: harvests - aggregated final harvest summaries (legacy model kept for reporting/back-compat). Persists models/harvest.py Harvest. |
| `inventory_asset` | `src/modules/farm_manager/services/database.py` | MongoDB collection: inventory_asset - farm equipment/assets with status, allocation and maintenance tracking. Persists models/inventory.py AssetInventory. |
| `inventory_harvest` | `src/modules/farm_manager/services/database.py` | MongoDB collection: inventory_harvest - SELLABLE harvest stock only, with quality grade, reservation and FIFO fields. Persists models/inventory.py HarvestInventory. |
| `inventory_input` | `src/modules/farm_manager/services/database.py` | MongoDB collection: inventory_input - farm input supplies (fertilizer, pesticide, seed, soil...) with base-unit normalization and low-stock flags. Persists models/inventory.py InputInventory. |
| `inventory_movements` | `src/modules/farm_manager/services/database.py` | MongoDB collection: inventory_movements - append-only ledger of additions, removals, adjustments, transfers, sales, waste, reservations and shipments. Persists models/inventory.py InventoryMovement. |
| `inventory_returned` | `src/modules/farm_manager/services/inventory/returned_repository.py` | MongoDB collection: inventory_returned - goods returned by customers (customer-origin, unlike farm-origin inventory_harvest). Persists models/inventory.py ReturnedInventory. |
| `inventory_waste` | `src/modules/farm_manager/services/database.py` | MongoDB collection: inventory_waste - waste ledger. Persists models/inventory.py WasteInventory. Pre-existing write paths (returns, expired, damaged, order deletion) are unchanged; Stage 3 added optional productId + harvestBatchId for waste-category harvest lines routed straight here from POST .../harvests/batch, plus indexes on harvestBatchId and sourceBlockId for the batch-lookup endpoint. |
| `plant_data` | `src/modules/farm_manager/services/plant_data/plant_data_repository.py` | MongoDB collection: plant_data - simple/legacy plant catalog. Persists models/plant_data.py PlantData. |
| `plant_data_enhanced` | `src/modules/farm_manager/services/plant_data/plant_data_enhanced_repository.py` | MongoDB collection: plant_data_enhanced - the VARIETY level of the two-level Plant Library (cultivation recipe: growth cycle, fertigation, environment, spacing, yield). Persists models/plant_data_enhanced.py PlantDataEnhanced; motherPlantId (indexed) + varietyName link each row up to its PlantMother. |
| `plant_mothers` | `src/modules/farm_manager/services/plant_data/plant_mother_repository.py` | MongoDB collection: plant_mothers - the MOTHER (product/SKU) level of the Plant Library; harvest/inventory/sales roll up here. Persists models/plant_mother.py PlantMother. Product extension Stage 1 embedded a products[] picklist of PlantProduct (productId/name/unit/category), indexed on products.productId; deletion is deactivation only. |
| `plantings` | `src/modules/farm_manager/services/planting/planting_repository.py` | MongoDB collection: plantings - planting plans with block assignments. Persists models/planting.py Planting. |
| `processing_inventory` | `src/modules/farm_manager/services/database.py` | MongoDB collection: processing_inventory - NEW (Plant Library product extension Stage 3, design doc §4.4). Destination for `process`-category harvest lines, deliberately separate from inventory_harvest which stays sellable-only. Persists models/inventory.py ProcessingInventory. Graded (A/B/C) but has no reservation/FIFO machinery. Indexed on inventoryId (unique), organizationId, farmId, blockId, productId, harvestBatchId, harvestDate, createdAt. |
| `products` | `src/modules/farm_manager/services/database.py` | MongoDB collection: products - master product catalog for inventory (organization-wide). Persists models/inventory.py Product. Distinct from PlantMother.products[] (the Plant Library per-mother picklist), which is embedded, not a collection. |
| `sensehub_alerts_cache` | `src/modules/farm_manager/services/sensehub/sync_service.py:243` | MongoDB collection: sensehub_alerts_cache - cached SenseHub alerts per block for offline fallback. |
| `sensehub_equipment_cache` | `src/modules/farm_manager/services/sensehub/sync_service.py:207` | MongoDB collection: sensehub_equipment_cache - daily snapshot of SenseHub equipment per block, read by SenseHubCacheQueryService when live SenseHub is unreachable. |
| `sensehub_lab_cache` | `src/modules/farm_manager/services/sensehub/sync_service.py:225` | MongoDB collection: sensehub_lab_cache - cached SenseHub lab readings; queried latest-per-metric and paginated by SenseHubCacheQueryService. |
| `sensehub_snapshots_cache` | `src/modules/farm_manager/services/sensehub/sync_service.py:261` | MongoDB collection: sensehub_snapshots_cache - cached SenseHub camera snapshot metadata per block. |
| `sensehub_sync_log` | `src/modules/farm_manager/services/sensehub/sync_service.py:279` | MongoDB collection: sensehub_sync_log - per-run SenseHub sync history (counts, errors, duration) surfaced by get_sync_history/get_status. |
| `stock_inventory` | `src/modules/farm_manager/services/database.py` | MongoDB collection: stock_inventory - farm stock/harvest inventory for FIFO tracking. Persists models/stock_inventory.py StockInventoryItem. |
| `system_config` | `src/modules/farm_manager/services/config_service.py` | MongoDB collection: system_config - singleton config documents; holds the farming year config (models/farming_year_config.py FarmingYearConfig) and the plant spacing standards (models/spacing_standards.py SpacingStandardsConfig). |
| `watchdog_notifications` | `src/modules/farm_manager/services/watchdog/service.py:39` | MongoDB collection: watchdog_notifications - history of watchdog issues that passed the cooldown filter and were sent to Telegram; also backs the per-issue cooldown check. |
| `weather_cache` | `src/modules/farm_manager/services/weather/weather_cache_service.py` | MongoDB collection: weather_cache - caches WeatherBit API responses. Persists models/weather.py WeatherCacheEntry. |

### Module: `finance`

| Collection/Model | File | Description |
|------------------|------|-------------|
| `financial_summary` | `scripts/data_import/2026_04_07/stage7_finalize.py` | MongoDB collection: financial_summary - pre-aggregated farm x crop x month P&L rollup produced by the 2026-04-07 import finaliser (stage7_finalize.py). Fields: summaryId, farmId, cropName, yearMonth, revenueAED, costAllocatedAED, grossMarginAED, divisionId, organizationId, generatedAt, metadata.migratedFrom. LIVE: 543 docs, indexed on (farmId, cropName, yearMonth). Written only by the migration script - no runtime service reads or refreshes it, so it is stale-by-design snapshot data. |
| `purchase_register` | `src/modules/finance/services/database.py` | MongoDB collection: purchase_register - flattened cost/voucher fact table for the ops P&L (indexes declared in finance/services/database.py). Fields: voucherId, vchNo, vchType, date, supplier, buyerEntity, items[] (with mappedCropName), purchasesSubtotal, vatAmount, totalAmount, source, divisionId, organizationId, metadata.legacyRef. LIVE: 130 docs with 4 declared indexes: metadata.legacyRef, date, (buyerEntity,date), items.mappedCropName. Aggregated in four places by finance.pnl_service. |
| `sales_order_lines` | `src/modules/finance/services/database.py` | MongoDB collection: sales_order_lines - flattened, per-line revenue fact table backing the ops P&L (indexes declared in finance/services/database.py; populated by the 2026-04-07 Excel import and its stage5b price-imputation pass). Fields: lineId, orderId/orderRef, customerId/customerName, farmId/farmName, blockId, divisionId, organizationId, farmingYear, cropName, grade, packageType/packageSize, quantity, totalKg, avgPrice, totalPrice, excel_data, metadata.priceSource, metadata.legacyRef. LIVE: 13,281 docs with 5 declared indexes: (farmId,farmingYear), metadata.priceSource, orderRef, (createdAt,farmId), metadata.legacyRef. Note this is the FINANCE module's fact table, unrelated to the embedded lines[] on sales_orders_v2. |

### Module: `finance_bridge`

| Collection/Model | File | Description |
|------------------|------|-------------|
| `finance_outbox` | `src/modules/finance_bridge/outbox_repository.py` | MongoDB collection: finance_outbox - transactional outbox queuing ops-side events for the statutory finance microservice (_COLLECTION in both outbox_repository.py and outbox_writer.py). Fields: eventId, eventType, status, payload, organizationId, companyCode, sourceDocumentId, sourceUserId, occurredAt, createdAt, attempts, lastAttemptAt, lastError, processedAt. Writes are gated by organizations.modules.financeEnabled and env_FINANCE_OUTBOX_ENABLED; the event type must be in EVENT_TYPE_REGISTRY or publish() raises. LIVE: 27 docs, with both declared indexes applied: ix_outbox_status_created (status, createdAt) and ix_outbox_eventId_unique (eventId). |

### Module: `genetics`

| Collection/Model | File | Description |
|------------------|------|-------------|
| `Accession models` | `src/modules/genetics/models/accession.py:1` | T-800 Physical material carrying dual generation counters (cloneGeneration G, filialGeneration F) and a parents list supporting 0, 1 or 2 entries with independently nullable accessionIds for half-known ancestry. generationLabel is a computed field, never persisted. T-804: publicToken (secrets-based Crockford base32, unique, backing the public label-info route), labelledVesselCount (a high-water mark that is NEVER decremented by split — the sticker is physical and permanent, the record is not) and sourceVesselNumbers. T-805: ParentRef.vesselNo records which physical vessel of a parent batch a propagation/split was taken from — same field/shape/validation reused on ObservationBase (T-805b). | Accession, AccessionCreate, AccessionUpdate, AccessionSplit, ParentRef, StorageL |
| `Genetic line models` | `src/modules/genetics/models/line.py:1` | T-800 The named identity, spanning plants, fungi and animals. Provenance records unknown origin as a state rather than a blank field. | Line, LineCreate, LineUpdate, LineStats, LineWithStats, Provenance, Trait |
| `Genetics enumerations` | `src/modules/genetics/models/enums.py:1` | T-800 Shared vocabulary. _SEXUAL_METHODS is the single source of truth for generation numbering; PropagationMethod.reproduction_mode and .max_parents derive from it. | OrganismKind, ProvenanceType, DerivationType, VesselForm, AccessionStatus, Paren |
| `Lineage graph models` | `src/modules/genetics/models/lineage.py:1` | T-800 Flat nodes+edges response shape for the DAG, plus the linear ancestry breadcrumb with branching and unknown-origin flags. T-805: LineageEdge.kind distinguishes a propagation-derived edge from a split-derived edge, since split() copies generations/parents verbatim and is not a propagation. | LineageGraph, LineageNode, LineageEdge, AncestryChain, AncestryStep |
| `Medium recipe & batch models` | `src/modules/genetics/models/medium.py:1` | T-800 Versioned formulations plus per-pour batches. Additives are modelled apart from base ingredients so trialled elements stay queryable; batches carry ingredientsSnapshot/additivesSnapshot. | Recipe, RecipeCreate, RecipeUpdate, Batch, BatchCreate, BatchUpdate, BatchQC, In |
| `Observation models` | `src/modules/genetics/models/observation.py:1` | T-800 Dated notes with optional quantitative metrics. isNovelTrait is what makes an observation promotable into its own line. T-805b: ObservationBase.vesselNo records which physical vessel of the observed accession the note applies to — same field/shape/validation as ParentRef.vesselNo (T-805a); capture-only, not yet surfaced anywhere in the UI. | Observation, ObservationCreate, ObservationUpdate, ObservationMetrics, PromoteTr |
| `Propagation models` | `src/modules/genetics/models/propagation.py:1` | T-800 The traceability edge: method, operator, date and medium alongside the parent pointers. reproductionMode is stored so historic events survive enum changes. T-808: amendedAt/amendedBy record when/who corrected performedAt; PropagationAmend is the single-field (performedAt-only) request model, PropagationAmendResult wraps the updated event plus accessionsUpdated/accessionsSkipped counts. | PropagationEvent, PropagationCreate, PropagationTarget, PropagationResult, Propa |
| `genetic_accessions` | `src/modules/genetics/services/database.py` | MongoDB collection: genetic_accessions - physical material with dual G/F generation counters, batch quantity and parents[]. Unique on accessionId and accessionCode; parents.accessionId indexed for lineage traversal. |
| `genetic_lines` | `src/modules/genetics/services/database.py` | MongoDB collection: genetic_lines - named genetic identities (strain/variety/bloodline) across plants, fungi and animals. Unique on lineId and code; indexed on kind, parentLineId, tags. |
| `genetic_observations` | `src/modules/genetics/services/database.py` | MongoDB collection: genetic_observations - dated notes per accession with metrics, novel-trait flag and promotedToLineId back-reference. |
| `medium_batches` | `src/modules/genetics/services/database.py` | MongoDB collection: medium_batches - one document per pour, snapshotting the recipe formulation so later recipe edits never rewrite history. |
| `medium_recipes` | `src/modules/genetics/services/database.py` | MongoDB collection: medium_recipes - versioned agar/substrate formulations. additives.name and ingredients.name indexed for the experiment readout. |
| `propagation_events` | `src/modules/genetics/services/database.py` | MongoDB collection: propagation_events - the clone/cross audit edge recording method, reproduction mode, operator, date and medium batch. |

### Module: `hr`

| Collection/Model | File | Description |
|------------------|------|-------------|
| `Contract` | `src/modules/hr/models/contract.py:59` | Employment contract model with type, status, salary. | Contract, ContractCreate, ContractType |
| `Employee` | `src/modules/hr/models/employee.py:116` | Employee model with Arabic name support, Emirates ID, emergency contacts. | Employee, EmployeeCreate, EmployeeStatus |
| `employee_contracts` | `src/modules/hr/services/employee/contract_repository.py` | MongoDB collection: employee_contracts - employment contract records |
| `employee_insurance` | `src/modules/hr/services/employee/insurance_repository.py` | MongoDB collection: employee_insurance - employee insurance policy records |
| `employee_performance` | `src/modules/hr/services/employee/performance_repository.py` | MongoDB collection: employee_performance - performance review records |
| `employee_visas` | `src/modules/hr/services/employee/visa_repository.py` | MongoDB collection: employee_visas - visa and work permit tracking (UAE) |
| `employees` | `src/modules/hr/services/employee/employee_repository.py` | MongoDB collection: employees - HR employee records with Arabic name support |
| `payroll_entries` | `scripts/data_import/2026_04_07/stage8_hr_payroll.py` | MongoDB collection: payroll_entries - per-employee payroll line for a payroll_runs.runId, created by the 2026-04-07 HR payroll import (stage8_hr_payroll.py). Fields: entryId, runId, employeeId, employeeName, grossSalary, deductions, netSalary, totalSalary, farmId/farmName, paymentType, paymentCompany, status, createdBy, divisionId, organizationId, metadata{migratedFrom, legacyRef, sourceSheet}. LIVE: 0 docs with a non-unique metadata.legacyRef index. No src/ module reads or writes it. |
| `payroll_runs` | `scripts/data_import/2026_04_07/stage8_hr_payroll.py` | MongoDB collection: payroll_runs - one document per payroll period, created by the 2026-04-07 HR payroll import (stage8_hr_payroll.py). Fields: runId, runDate, period{year,month,label}, totalAmount, totalEmployees, byFarm, byCompany, byPaymentType, status, createdBy, divisionId, organizationId, metadata{migratedFrom, legacyRef, sourceSheets[]}. LIVE: 0 docs but the collection exists with its unique metadata.legacyRef index - the import created indexes then rolled back or was never run to completion. No src/ module reads or writes it: the HR module has no payroll service. |

### Module: `logistics`

| Collection/Model | File | Description |
|------------------|------|-------------|
| `Shipment` | `src/modules/logistics/models/shipment.py:76` | Shipment model with cargo items, tracking, and order assignment. | Shipment, ShipmentCreate, ShipmentStatus, CargoItem |
| `routes` | `src/modules/logistics/services/logistics/route_repository.py` | MongoDB collection: routes - delivery route definitions with stops |
| `shipments` | `src/modules/logistics/services/logistics/shipment_repository.py` | MongoDB collection: shipments - shipment tracking records |
| `vehicles` | `src/modules/logistics/services/logistics/vehicle_repository.py` | MongoDB collection: vehicles - fleet vehicle records with maintenance tracking |

### Module: `marketing`

| Collection/Model | File | Description |
|------------------|------|-------------|
| `Campaign` | `src/modules/marketing/models/campaign.py:67` | Marketing campaign model with status, metrics, and channel assignments. | Campaign, CampaignCreate, CampaignStatus, CampaignMetrics |
| `marketing_budgets` | `src/modules/marketing/services/marketing/budget_repository.py` | MongoDB collection: marketing_budgets - marketing budget allocations |
| `marketing_campaigns` | `src/modules/marketing/services/marketing/campaign_repository.py` | MongoDB collection: marketing_campaigns - marketing campaign records |
| `marketing_channels` | `src/modules/marketing/services/marketing/channel_repository.py` | MongoDB collection: marketing_channels - marketing channel definitions |
| `marketing_events` | `src/modules/marketing/services/marketing/event_repository.py` | MongoDB collection: marketing_events - marketing event records |

### Module: `mushroom_manager`

| Collection/Model | File | Description |
|------------------|------|-------------|
| `Contamination Report models` | `src/modules/mushroom_manager/models/contamination.py:1` | Contamination incident record. Three enums: ContaminationType (trichoderma/cobweb_mold/bacteria/black_mold/green_mold/lipstick_mold/other), ContaminationSeverity (low/medium/high/critical) and ContaminationAction (monitoring/treatment/quarantine/disposal, default monitoring). The quarantined boolean on create is what triggers the room's forced phase override. Resolution state (isResolved/resolvedAt/resolvedBy/resolutionNotes) lives on the report itself; ContaminationResolveRequest is the PATCH payload. | ContaminationType, ContaminationSeverity, ContaminationAction, ContaminationRepo |
| `Environment Log models` | `src/modules/mushroom_manager/models/environment.py:1` | A point-in-time room climate reading: temperature (C), humidity %, co2Level (ppm), lightLevel (lux), freshAirFlow — every field optional, so a partial reading from one sensor is valid. The stored EnvironmentLog denormalises the room's currentPhase at recording time and carries an isOutOfRange alert flag that no code currently computes. No enums in this file. | EnvironmentReading, EnvironmentLogCreate, EnvironmentLog |
| `Facility models` | `src/modules/mushroom_manager/models/facility.py:1` | The top-level cultivation container. Enums: FacilityType (indoor/greenhouse/outdoor/hybrid/container/cave) and FacilityStatus (active/inactive/maintenance/construction), both written to match the frontend union types exactly. The document model exposes 'id' (renamed to facilityId in Mongo by the service) and carries totalRooms/activeRooms counters plus manager and division/organization scoping. FacilityLocation is dead weight kept only so legacy structured-location documents still decode — the live surface stores location as a plain string. | FacilityType, FacilityStatus, FacilityLocation, FacilityBase, FacilityCreate, Fa |
| `Growing Room models + lifecycle tables` | `src/modules/mushroom_manager/models/growing_room.py:1` | The module's rulebook, not just a schema. RoomType (lab/spawn/substrate_prep/incubation/fruiting/storage/harvest_pack) decides which model applies: BATCH_ROOM_TYPES is {FRUITING} alone — only a fruiting room runs one crop through a lifecycle, everything else is a container holding independently-tracked items and may only hold OPERATIONAL_PHASES (empty/cleaning/quarantined/maintenance/decommissioned), resolved by allowed_phases_for(). RoomPhase is the 12-state lifecycle and VALID_TRANSITIONS the adjacency table, whose cycle empty→preparing→inoculated→colonizing→fruiting_initiation→fruiting→harvesting→resting→(fruiting_initiation | cleaning)→empty is what makes multi-flush cropping expressible; DECOMMISSIONED is terminal. Also ClimateSettings (per-phase temp/humidity/CO2/light/air-exchange targets, stored as a phase-keyed dict), FlushInfo (currentFlush/totalFlushes/maxFlushes, default max 4) and PhaseHistoryEntry. The document exposes 'id' (roomId in Mongo) and denormalises biologicalEfficiency and totalYieldKg. | RoomType, BATCH_ROOM_TYPES, RoomPhase, VALID_TRANSITIONS, OPERATIONAL_PHASES, al |
| `Harvest models` | `src/modules/mushroom_manager/models/harvest.py:1` | Flush-aware harvest record. QualityGrade enum is A/B/C/reject. The interesting half is lineage attribution: strainId answers 'what species', while accessionId/accessionCode/lineId/lineCode/cloneGeneration/filialGeneration (denormalised from the genetics collections at write time) answer 'which of my cultures produced it, at which generation'. protocolRef pins the SOP actually followed as {protocolId, code, title, version, followedAt}. substrateWeightKg may override the room-level figure so per-block biological efficiency stays comparable when a room holds blocks from several batches. On create, flushNumber is optional and auto-filled from the room. | QualityGrade, HarvestBase, HarvestCreate, Harvest |
| `Strain models` | `src/modules/mushroom_manager/models/strain.py:1` | The strain library entry — species-level cultivation reference, the mushroom analogue of Plant Data. DifficultyLevel enum (beginner/intermediate/advanced/expert). Requirements are deliberately FLAT fields (colonizationTempMin/Max, colonizationHumidityMin, colonizationDaysMin/Max, the matching fruiting* set, co2TolerancePpm, expectedYieldKgPerKgSubstrate, maxFlushes) named to match the frontend contract so no client-side transformation is needed. PhaseRequirement and SubstrateIngredient are the nested shapes that flattening replaced — declared but unused. Document exposes 'id' (strainId in Mongo) and isActive as the retirement switch. | DifficultyLevel, PhaseRequirement, SubstrateIngredient, StrainBase, StrainCreate |
| `Substrate Batch models` | `src/modules/mushroom_manager/models/substrate.py:1` | Substrate preparation and sterilization tracking. SubstrateStatus enum (preparing/sterilized/loaded/spent/disposed) is the batch's own lifecycle, distinct from the room phase; SterilizationMethod enum (autoclave/pasteurization/cold_pasteurization/chemical/other) — note this name collides with an identically-named enum in genetics.model.enums, different module and different members. RawMaterial captures name/weight/supplier/cost per ingredient, giving a per-batch input cost trail. Unlike the other document models this one keeps its native batchId field rather than exposing 'id'. | SubstrateStatus, SterilizationMethod, RawMaterial, SubstrateBatchBase, Substrate |
| `contamination_reports` | `src/modules/mushroom_manager/services/database.py:125` | MongoDB collection: contamination_reports — incident records with resolution state. Unique on reportId; indexed on roomId, facilityId, contaminationType, severity, isResolved, reportedAt desc, createdAt desc. The isResolved index backs the dashboard's active-alerts query. |
| `growing_rooms` | `src/modules/mushroom_manager/services/database.py:68` | MongoDB collection: growing_rooms — climate-controlled rooms with the 12-phase lifecycle, embedded phaseHistory[] and flushInfo. Unique on roomId and on the compound (facilityId, roomCode); indexed on facilityId, currentPhase, strainId, substrateBatchId, createdAt desc. Document key is roomId while the API model calls it id. |
| `mushroom_facilities` | `src/modules/mushroom_manager/services/database.py:61` | MongoDB collection: mushroom_facilities — top-level cultivation sites. Unique on facilityId; indexed on managerId, status, facilityType and createdAt desc. Note the document key is facilityId while the API model calls it id. |
| `mushroom_harvests` | `src/modules/mushroom_manager/services/database.py:94` | MongoDB collection: mushroom_harvests — flush-aware harvest events carrying denormalised genetics lineage and a pinned protocolRef. Unique on harvestId; indexed on roomId, facilityId, flushNumber, qualityGrade, harvestedAt desc, and compound (roomId, flushNumber) / (facilityId, harvestedAt desc). Lineage indexes accessionId, lineId and (lineId, cloneGeneration) exist specifically to back the yield-by-line rollup. |
| `mushroom_strains` | `src/modules/mushroom_manager/services/database.py:80` | MongoDB collection: mushroom_strains — the global (non-facility-scoped) strain catalogue with flat climate-requirement fields. Unique on strainId; indexed on commonName, difficultyLevel, isActive, createdAt desc. Document key is strainId while the API model calls it id. |
| `room_environment_logs` | `src/modules/mushroom_manager/services/database.py:114` | MongoDB collection: room_environment_logs — append-only climate readings with the room's phase denormalised at write time. Unique on logId; indexed on roomId, facilityId, isOutOfRange, recordedAt desc, and compound (roomId, recordedAt desc) for the latest-reading query. |
| `substrate_batches` | `src/modules/mushroom_manager/services/database.py:87` | MongoDB collection: substrate_batches — substrate preparation and sterilization records with embedded rawMaterials[] and an assignedRooms[] list. Unique on batchId; indexed on facilityId, status, batchCode, createdAt desc. batchCode uniqueness per facility is enforced in the service, not by an index. |

### Module: `protocols`

| Collection/Model | File | Description |
|------------------|------|-------------|
| `protocols` | `src/modules/protocols/services/database.py` | MongoDB collection: protocols - SOP/protocol library (PROTOCOLS in protocols/services/database.py). Fields: protocolId, code, title, version, category, scope, status, purpose, steps[], materials[], equipment[], ppe[], safetyNotes, references, referenceImages, appliesTo[], tags[], approvedBy/approvedByName/approvedAt, divisionId, organizationId, notes. LIVE: 12 docs with all 9 declared indexes applied: protocolId (unique), code (unique), category, status, tags, appliesTo, (appliesTo,status), createdAt desc. Cross-module readers: genetics.protocol_link and genetics.api.v1.public (_PROTOCOLS), mushroom_manager.harvest_service. |

### Module: `purchasing`

| Collection/Model | File | Description |
|------------------|------|-------------|
| `payment_terms` | `src/modules/purchasing/services/payment_terms_service.py` | MongoDB collection: payment_terms - shared payment-terms master (_COLLECTION in purchasing/services/payment_terms_service.py). Fields: termsId, termsCode, description, netDays, isActive, organizationId, createdBy, created/updated stamps. Cross-module: sales.ar_invoice_service reads it as _PAYMENT_TERMS_COL to compute AR invoice dueDate, and document_headers stores paymentTermsCode. LIVE: 9 docs, NO indexes beyond _id. |
| `purchase_items` | `src/modules/purchasing/services/purchase_item_service.py` | MongoDB collection: purchase_items - the purchasing item master (_COLLECTION in purchase_item_service.py). Fields: itemId, itemCode, name, description, itemType, uom, barcode, manufacturer, defaultUnitCost, defaultWarehouseId, organizationId, isActive, deletedAt (soft delete), created/updated stamps. Resolved by purchasing.document_service when building document_lines. LIVE: 6 docs, NO indexes beyond _id - itemCode uniqueness is enforced in application code only. |
| `vendors` | `src/modules/purchasing/services/vendor_service.py` | MongoDB collection: vendors - business-partner master for purchasing (_COLLECTION in vendor_service.py). Fields: vendorId, vendorCode, name, contactName/contactEmail/contactPhone, addressLine1/2, city, country, trn (tax registration number), currencyCode, paymentTermsCode, creditLimit, bankDetails, isActive, isBlocked, notes, organizationId, deletedAt. Denormalised onto document_headers as vendorId/vendorCode/vendorName. LIVE: 4 docs, NO indexes beyond _id. |

### Module: `sales`

| Collection/Model | File | Description |
|------------------|------|-------------|
| `AR Credit Note models` | `src/modules/sales/models/ar_credit_notes.py:1` | Pydantic schemas for AR Credit Note + allocations (T-100.11). | ARCreditNoteCreate, ARCreditNoteUpdate, ARCreditNoteResponse, ARCreditNoteListIt |
| `AR Invoice models` | `src/modules/sales/models/ar_invoices.py:1` | Pydantic schemas for AR Invoice document, lines, totals, transitions (T-100.9a). | ARInvoiceCreate, ARInvoiceUpdate, ARInvoiceResponse, ARInvoiceListItem, ARInvoic |
| `Customer Receipt models` | `src/modules/sales/models/customer_receipts.py:1` | Pydantic schemas for Customer Receipt + allocation entries (T-100.10). | CustomerReceiptCreate, CustomerReceiptUpdate, CustomerReceiptResponse, CustomerR |
| `Delivery Note models` | `src/modules/sales/models/deliveries.py:1` | Pydantic schemas for Delivery Note document and lines (T-100.8). | DeliveryCreate, DeliveryUpdate, DeliveryResponse, DeliveryListItem, DeliveryLine |
| `Purchase Order (legacy stub)` | `src/modules/sales/models/purchase_order.py:1` | Legacy PO schemas retained for dashboard. Sales-side PO routes were removed in T-070.0; current POs are in purchasing module. | PurchaseOrder, PurchaseOrderCreate, PurchaseOrderUpdate, PurchaseOrderStatus, Pu |
| `Quote models` | `src/modules/sales/models/quotes.py:1` | Pydantic schemas for Sales Quote document, lines, totals, transitions (T-100.6). | QuoteCreate, QuoteUpdate, QuoteResponse, QuoteListItem, QuoteLineCreate, QuoteLi |
| `Return Note v2 models` | `src/modules/sales/models/returns.py:1` | Pydantic schemas for Return Note (RTN) v2 document and lines (T-100.11). | ReturnCreate, ReturnUpdate, ReturnResponse, ReturnListItem, ReturnLineCreate, Re |
| `Return Order (legacy) models` | `src/modules/sales/models/return_order.py:1` | Legacy return-order schemas with reason/condition enums and process-return payloads. | ReturnOrder, ReturnOrderCreate, ReturnOrderUpdate, ReturnStatus, ReturnReason, R |
| `Return Request models` | `src/modules/sales/models/return_requests.py:1` | Pydantic schemas for Return Request (RMA) document and lines (T-100.11). | ReturnRequestCreate, ReturnRequestUpdate, ReturnRequestResponse, ReturnRequestLi |
| `Sales Order (legacy) models` | `src/modules/sales/models/sales_order.py:1` | Legacy Pydantic schemas for sales_orders collection. Status enum, allocation, delete preview, report-return flows. | SalesOrder, SalesOrderCreate, SalesOrderUpdate, SalesOrderStatus, PaymentStatus, |
| `Sales Order v2 models` | `src/modules/sales/models/sales_orders.py:1` | Pydantic schemas for Sales Order v2 (T-100.7) including credit-check snapshot and from-quote request. | SalesOrderCreate, SalesOrderUpdate, SalesOrderResponse, SalesOrderListItem, Sale |
| `ar_credit_notes_v2` | `src/modules/sales/services/ar_credit_note_service.py` | MongoDB collection: ar_credit_notes_v2 - Wave 3 AR Credit Note (docType AR_CREDIT_NOTE), issued against an AR Invoice and optionally a Return. Fields: docEntry, docNumber, creditReason/creditReasonText, allocations[] (against invoices), baseReturnDocRef, customerId/customerName, currency/exchangeRate, docDate/invoiceDate/taxDate/dateOfSupply, lines[], totals, journalMemo, targetDocRefs, outboxEventId/outboxEventEmittedAt. Owned by sales.service.ar_credit_note_service. LIVE: 1 doc, NO indexes beyond _id. |
| `ar_credit_notes_v2_audit` | `src/modules/sales/services/ar_credit_note_service.py` | MongoDB collection: ar_credit_notes_v2_audit - append-only audit trail for AR Credit Notes. Fields: docEntry, action, detail, userId, timestamp. LIVE: 2 docs, no indexes. |
| `ar_invoices_v2` | `src/modules/sales/services/ar_invoice_service.py` | MongoDB collection: ar_invoices_v2 - Wave 3 AR Invoice (docType AR_INVOICE). Fields: docEntry, docNumber, organizationId, companyCode, customerId/customerName, bpRefNo, currency/exchangeRate, docDate, invoiceDate, dueDate, taxDate, dateOfSupply, paymentTermsId, isCashSale, isReserveInvoice, status, lines[] (taxCodeId FK to the finance service's tax_codes, not a Mongo collection), totals, journalMemo, baseDocRef, targetDocRefs, outboxEventId/outboxEventEmittedAt. Owned by sales.service.ar_invoice_service; read by ar_credit_note_service, customer_receipt_service, reports_service. LIVE: 5 docs, NO indexes beyond _id. |
| `ar_invoices_v2_audit` | `src/modules/sales/services/ar_invoice_service.py` | MongoDB collection: ar_invoices_v2_audit - append-only audit trail for AR Invoices. Fields: docEntry, action, detail, userId, timestamp. LIVE: 15 docs, no indexes. |
| `customer_receipts_v2` | `src/modules/sales/services/customer_receipt_service.py` | MongoDB collection: customer_receipts_v2 - Wave 3 Customer Receipt / incoming payment (docType CUSTOMER_RECEIPT). Fields: docEntry, docNumber, customerId/customerName, amountReceived, unallocatedAmount, allocations[] (per AR invoice), bankAccountId, paymentMethod, paymentRef, currency/exchangeRate, baseDocRefs[], targetDocRefs, journalMemo, outboxEventId/outboxEventEmittedAt. Owned by sales.service.customer_receipt_service. LIVE: 4 docs, NO indexes beyond _id. |
| `customer_receipts_v2_audit` | `src/modules/sales/services/customer_receipt_service.py` | MongoDB collection: customer_receipts_v2_audit - append-only audit trail for Customer Receipts. Fields: docEntry, action, detail, userId, timestamp. LIVE: 7 docs, no indexes. |
| `deliveries_v2` | `src/modules/sales/services/delivery_service.py` | MongoDB collection: deliveries_v2 - Wave 3 Delivery Note (docType DELIVERY); consumes SO open quantity and books COGS. Fields: docEntry, docNumber, organizationId, companyCode, customerId/customerName, docDate, actualDeliveryDate, deliveredByUserId, status, lines[], totalCogs, baseDocRef (SO), targetDocRefs, outboxEventId/outboxEventEmittedAt (finance_bridge handoff). Owned by sales.service.delivery_service. LIVE: 4 docs, NO indexes beyond _id. |
| `deliveries_v2_audit` | `src/modules/sales/services/delivery_service.py` | MongoDB collection: deliveries_v2_audit - append-only audit trail for Delivery Notes. Fields: docEntry, action, detail, userId, timestamp. LIVE: 11 docs, no indexes. |
| `purchase_orders` | `src/modules/sales/services/sales/purchase_order_repository.py` | MongoDB collection: purchase_orders - LEGACY purchase order living in the sales module (sales.model.legacy.purchase_order), predating the Wave 0 document_headers/document_lines chain. Indexes declared in sales/services/database.py: purchaseOrderId (unique), poCode (unique), supplierId, supplierName, status, orderDate, expectedDeliveryDate, createdBy, createdAt desc, organizationId, divisionId and a purchase_order_search_text full-text index. LIVE: 0 docs - the collection is index-only; real purchasing now writes document_headers/document_lines. |
| `return_orders` | `src/modules/sales/services/sales/return_service.py` | MongoDB collection: return_orders - LEGACY return document for the pre-Wave-3 sales_orders chain (ReturnService.collection_name). LIVE: 0 docs, only divisionId and organizationId indexes - superseded by returns_v2 / return_requests_v2. |
| `return_requests_v2` | `src/modules/sales/services/return_request_service.py` | MongoDB collection: return_requests_v2 - Wave 3 Return Request (docType RETURN_REQUEST), the customer-facing precursor to a Return. Fields: docEntry, docNumber, customerId/customerName, docDate, validUntilDate, reason/reasonText, lines[], totals, baseDocRef (delivery), targetDocRefs, status. Owned by sales.service.return_request_service; read by rtn_service (_RR_COL). LIVE: 4 docs, NO indexes beyond _id. |
| `return_requests_v2_audit` | `src/modules/sales/services/return_request_service.py` | MongoDB collection: return_requests_v2_audit - append-only audit trail for Return Requests. Fields: docEntry, action, detail, userId, timestamp. LIVE: 6 docs, no indexes. |
| `returns_v2` | `src/modules/sales/services/rtn_service.py` | MongoDB collection: returns_v2 - Wave 3 Return / goods-back document (docType RETURN), reverses a Delivery and feeds an AR Credit Note. Fields: docEntry, docNumber, customerId/customerName, docDate, actualReturnDate, receivedByUserId, lines[], totals, baseDocRef (delivery or return request), targetDocRefs, outboxEventId/outboxEventEmittedAt. Owned by sales.service.rtn_service. LIVE: 1 doc, NO indexes beyond _id. |
| `returns_v2_audit` | `src/modules/sales/services/rtn_service.py` | MongoDB collection: returns_v2_audit - append-only audit trail for Returns. Fields: docEntry, action, detail, userId, timestamp. LIVE: 2 docs, no indexes. |
| `sales_orders` | `src/modules/sales/services/sales/order_repository.py` | MongoDB collection: sales_orders - LEGACY (pre-Wave-3) sales order with embedded items[], the farm-facing order used by logistics shipments and the ops P&L. Fields: orderId, orderCode/orderNumber, customerId/customerName, farmId/farmName, divisionId, organizationId, farmingYear/viewingYear, items[], subtotal/tax/discount/total, status, paymentStatus, receiver, driverEmail/packerEmail, vehicleId/vehicleName, startDate/packedDate/finishedDate, metadata.legacyRef. LIVE: 4,649 docs (the real historical sales data) and 15 indexes incl. a sales_order_search_text full-text index; indexes declared in sales/services/database.py. Read by finance.pnl_service, logistics.shipment_service and core.api.dashboard. |
| `sales_orders_v2` | `src/modules/sales/services/sales_order_service.py` | MongoDB collection: sales_orders_v2 - Wave 3 Sales Order document (docType SALES_ORDER), the hub every AR Invoice traces back to. Fields: docEntry, docNumber, organizationId, companyCode, customerId/customerName, bpRefNo, currency/exchangeRate, docDate, deliveryDate, paymentTermsId, salesEmployeeId, ownerUserId, creditCheck, status, lines[] (with open/closed quantity), totals, journalMemo, baseDocRef (source quote), targetDocRefs. Owned by sales.service.sales_order_service; also read by delivery_service (_SO_COL). LIVE: 5 docs, NO indexes beyond _id. |
| `sales_orders_v2_audit` | `src/modules/sales/services/sales_order_service.py` | MongoDB collection: sales_orders_v2_audit - append-only audit trail for Wave 3 Sales Orders. Fields: docEntry, action, detail, userId, timestamp. Read by GET /api/v1/sales/audit?docType=SALES_ORDER. LIVE: 13 docs, no indexes. |
| `sales_quotes` | `src/modules/sales/services/quote_service.py` | MongoDB collection: sales_quotes - Wave 3 Sales Quotation header+embedded-lines document (docType QUOTE). Fields: docEntry, docNumber, docType, organizationId, companyCode, customerId/customerName, bpRefNo, currency/exchangeRate, docDate, validUntilDate, paymentTermsId, salesEmployeeId, ownerUserId, status, lines[], totals, journalMemo, baseDocRef, targetDocRefs, created/updated audit stamps. Owned by sales.service.quote_service. LIVE: 1 doc, NO indexes beyond _id - the module declares none. |
| `sales_quotes_audit` | `src/modules/sales/services/quote_service.py` | MongoDB collection: sales_quotes_audit - append-only audit trail for Sales Quotations, written by quote_service._write_audit (_AUDIT_COL). Fields: docEntry, action, detail, userId, timestamp. LIVE: 5 docs, no indexes. WARNING: sales/api/v1/audit.py maps docType QUOTE to 'quotes_v2_audit', a collection that does not exist - quote audit history reads an empty collection. |
| `sales_unmatched` | `scripts/data_import/2026_04_07/stage5_sales_excel.py` | MongoDB collection: sales_unmatched - import-staging quarantine for Excel sales rows that could not be matched to a farm/crop/customer during the 2026-04-07 data migration (stage5_sales_excel.py, RESET_COLLECTIONS). Fields: unmatchedId, date/normalizedDate/collectionDate, customerName, cropName, grade, qtyCarton/qtyKgs/kgConversion, actualPrice, totalActualValue, vatAmount, totalAmountAfterTax, paidAmount/unpaidAmount, paymentStatus/invoiceStatus, deliveryCode, inventorySource, reason, metadata.legacyRef. LIVE: 3,328 docs. No application code reads it - it is a migration artefact reviewed by hand. |

## Service → Collection Access

| Service | Access | Collection | Context |
|---------|--------|------------|---------|
| `farm_manager.service.FarmAnalyticsService` | reads_from | `farm_manager.service.BlockRepository` | FarmAnalyticsService reads block data for analytics calculations. |
| `farm_manager.service.FarmAnalyticsService` | reads_from | `farm_manager.service.HarvestRepository` | FarmAnalyticsService reads harvest data for yield analytics. |
| `farm_manager.service.GlobalAnalyticsService` | reads_from | `farm_manager.service.FarmRepository` | GlobalAnalyticsService reads farm list to iterate across all farms. |
| `farm_manager.service.FarmRepository` | reads_from | `collection_farms` | self.collection_name = 'farms' |
| `farm_manager.service.BlockRepository` | reads_from | `collection_blocks` | db.blocks.find/insert/update |
| `farm_manager.service.PlantDataRepository` | reads_from | `collection_plant_data` | db.plant_data.find/insert/update |
| `farm_manager.service.PlantDataEnhancedRepository` | reads_from | `collection_plant_data_enhanced` | COLLECTION = 'plant_data_enhanced' |
| `farm_manager.service.PlantingRepository` | reads_from | `collection_plantings` | farm_db.db.plantings.find/insert |
| `farm_manager.service.HarvestRepository` | reads_from | `collection_block_harvests` | db.block_harvests |
| `farm_manager.service.AlertRepository` | reads_from | `collection_alerts` | db.alerts |
| `farm_manager.service.ArchiveRepository` | reads_from | `collection_block_archives` | db.block_archives |
| `TaskRepository` | reads_from | `collection_farm_tasks` | db.farm_tasks.find/insert/update |
| `farm_manager.service.ConfigService` | reads_from | `collection_system_config` | COLLECTION_NAME = 'system_config' |
| `farm_manager.service.FarmingYearService` | reads_from | `collection_system_config` | COLLECTION_NAME = 'system_config' |
| `farm_manager.service.WeatherCacheService` | reads_from | `collection_weather_cache` | COLLECTION_NAME = 'weather_cache' |
| `ai_analytics.service.CostTrackingService` | reads_from | `collection_ai_query_log` | self.collection = self.db['ai_query_log'] |
| `AuthService` | reads_from | `collection_users` | db.users.find_one/insert_one/update_one |
| `AuthService` | reads_from | `collection_refresh_tokens` | db.refresh_tokens.find_one/insert_one/update_one |
| `AuthService` | reads_from | `collection_mfa_pending_tokens` | db.mfa_pending_tokens.insert_one/find_one/update_one |
| `UserService` | reads_from | `collection_users` | db.users.find_one/count_documents/update_one |
| `MfaService` | reads_from | `collection_users` | db.users.find_one/update_one (MFA fields) |
| `hr.service.EmployeeRepository` | reads_from | `collection_employees` | self.collection_name = 'employees' |
| `ContractRepository` | reads_from | `collection_employee_contracts` | self.collection_name = 'employee_contracts' |
| `VisaRepository` | reads_from | `collection_employee_visas` | self.collection_name = 'employee_visas' |
| `InsuranceRepository` | reads_from | `collection_employee_insurance` | self.collection_name = 'employee_insurance' |
| `PerformanceRepository` | reads_from | `collection_employee_performance` | self.collection_name = 'employee_performance' |
| `crm.service.CustomerRepository` | reads_from | `collection_customers` | self.collection_name = 'customers' |
| `OrderRepository` | reads_from | `collection_sales_orders` | self.collection_name = 'sales_orders' |
| `InventoryRepository` | reads_from | `collection_harvest_inventory` | self.collection_name = 'harvest_inventory' |
| `PurchaseOrderRepository` | reads_from | `collection_purchase_orders` | self.collection_name = 'purchase_orders' |
| `ReturnService` | reads_from | `collection_return_orders` | self.collection_name = 'return_orders' |
| `VehicleRepository` | reads_from | `collection_vehicles` | self.collection_name = 'vehicles' |
| `RouteRepository` | reads_from | `collection_routes` | self.collection_name = 'routes' |
| `logistics.service.ShipmentRepository` | reads_from | `collection_shipments` | self.collection_name = 'shipments' |
| `CampaignRepository` | reads_from | `collection_marketing_campaigns` | self.collection_name = 'marketing_campaigns' |
| `BudgetRepository` | reads_from | `collection_marketing_budgets` | self.collection_name = 'marketing_budgets' |
| `ChannelRepository` | reads_from | `collection_marketing_channels` | self.collection_name = 'marketing_channels' |
| `EventRepository` | reads_from | `collection_marketing_events` | self.collection_name = 'marketing_events' |
| `genetics.service.line_service` | reads_from | `collection_genetic_accessions` | _bulk_stats aggregates accession rollups per line. |
| `genetics.service.medium_service` | reads_from | `collection_genetic_accessions` | find_accessions_by_additive walks additive -> batches -> accessions. |
| `genetics.service.lineage_service` | reads_from | `collection_genetic_accessions` | Breadth-first traversal over parents.accessionId, one query per depth level. |
| `genetics.service.dashboard_service` | reads_from | `collection_genetic_accessions` | Counts live material, vessels and the senescence watch list. |
| `genetics.service.maintenance_service` | reads_from | `collection_genetic_lines` | _existing_line_ids builds the reference set every other collection's lineId is d |
| `core.api.health` | reads_from | `core.service.database` | mongodb.health_check() for /health and /ready |
| `core.api.dashboard` | reads_from | `collection_farms` | farm count/active aggregation |
| `core.api.dashboard` | reads_from | `collection_blocks` | block count + state breakdown aggregation |
| `core.api.dashboard` | reads_from | `collection_employees` | employee count/active aggregation |
| `core.api.dashboard` | reads_from | `collection_customers` | customer count/active aggregation |
| `core.api.dashboard` | reads_from | `collection_sales_orders` | line 147: await db.sales_orders.count_documents({}) |
| `core.api.dashboard` | reads_from | `collection_vehicles` | vehicle count + status breakdown aggregation |
| `core.api.dashboard` | reads_from | `collection_shipments` | shipment count + status breakdown aggregation |
| `core.api.dashboard` | reads_from | `collection_campaigns` | campaign count/active aggregation |
| `core.api.dashboard` | reads_from | `collection_users` | user count/active aggregation |
| `core.api.modules` | reads_from | `collection_module_audit_log` | GET /modules/audit-log paginated + filtered query |
| `core.api.organizations` | reads_from | `collection_organizations` | get_organization / list_organizations |
| `core.service.deployment_settings_service` | reads_from | `collection_users` | verify actor's current password before applying changes |
| `core.service.division_service` | reads_from | `collection_organizations` | create_division validates the parent org exists |
| `finance_bridge.tenant_flag` | reads_from | `collection_organizations` | modules.financeEnabled projection on cache miss |
| `finance_bridge.outbox_repository` | reads_from | `collection_finance_outbox` | consumer worker status-transition queries via atomic findOneAndUpdate |
| `core.service.deployment_settings_service` | reads_from | `collection_platform_settings` | db.platform_settings.find_one/update_one({'_id': 'deployment'}) |
| `farm_manager.service.PlantMotherRepository` | reads_from | `collection_plant_mothers` | PlantMotherRepository reads/writes the 'plant_mothers' collection. |
| `ai_assistant.service.conversation_repository` | reads_from | `collection_ai_assistant_conversations` | get() find_one, list_summaries() find().sort(updated_at, -1), _evict_if_needed() |
| `farm_manager.api.inventory` | reads_from | `collection_processing_inventory` | GET /inventory/processing reads db.processing_inventory (written by the harvest  |
| `mushroom_manager.service.facility_service` | reads_from | `collection_growing_rooms` | delete_facility counts rooms in the facility before refusing with 409. |
| `mushroom_manager.service.facility_service` | reads_from | `collection_substrate_batches` | delete_facility counts substrate batches in the facility before refusing with 40 |
| `mushroom_manager.service.room_service` | reads_from | `collection_mushroom_facilities` | create_room validates the parent facility exists. |
| `mushroom_manager.service.room_service` | reads_from | `collection_genetic_accessions` | room_dependents counts accessions with location.roomId == this room — deleting t |
| `mushroom_manager.service.room_service` | reads_from | `collection_mushroom_harvests` | room_dependents counts harvests attached to the room. |
| `mushroom_manager.service.room_service` | reads_from | `collection_contamination_reports` | room_dependents counts contamination reports attached to the room. |
| `mushroom_manager.service.room_service` | reads_from | `collection_room_environment_logs` | room_dependents counts environment logs attached to the room. |
| `mushroom_manager.service.substrate_service` | reads_from | `collection_mushroom_facilities` | create_batch validates the parent facility exists. |
| `mushroom_manager.service.harvest_service` | reads_from | `collection_genetic_accessions` | Resolves the harvested block by accessionId and denormalises accessionCode / clo |
| `mushroom_manager.service.harvest_service` | reads_from | `collection_genetic_lines` | Looks up the accession's line to denormalise lineCode for the yield-by-line grou |
| `mushroom_manager.service.harvest_service` | reads_from | `collection_protocols` | Pins the cited SOP into protocolRef and rejects any protocol whose status is not |
| `mushroom_manager.service.environment_service` | reads_from | `collection_growing_rooms` | Validates the room and reads its currentPhase to denormalise onto the reading. |
| `mushroom_manager.api.dashboard` | reads_from | `collection_mushroom_facilities` | Global facility count and the per-facility analytics name/status lookup. |
| `mushroom_manager.api.dashboard` | reads_from | `collection_growing_rooms` | Room totals and the rooms-by-phase $group aggregation, globally and per facility |
| `mushroom_manager.api.dashboard` | reads_from | `collection_mushroom_harvests` | Last-10 recent harvests plus the yield / average-BE / per-flush aggregations. |
| `mushroom_manager.api.dashboard` | reads_from | `collection_contamination_reports` | Unresolved alert count and list, and the per-facility total/resolved/active spli |
| `farm_manager.service.HarvestService` | reads_from | `collection_block_harvests` | get_batch_lookup fans out over block_harvests + processing_inventory + inventory |
| `farm_manager.service.HarvestRepository` | reads_from | `collection_blocks` | create() resolves farmId from the block and recomputes block KPI rollups |
| `farm_manager.service.DataCollector` | reads_from | `farm_manager.service.FarmRepository` | one of the 9 parallel inspection tasks |
| `farm_manager.service.DataCollector` | reads_from | `farm_manager.service.BlockRepository` | one of the 9 parallel inspection tasks |
| `farm_manager.service.DataCollector` | reads_from | `farm_manager.service.HarvestRepository` | one of the 9 parallel inspection tasks |
| `farm_manager.service.DataCollector` | reads_from | `farm_manager.service.AlertRepository` | one of the 9 parallel inspection tasks |
| `farm_manager.service.DataCollector` | reads_from | `farm_manager.service.SenseHubCacheQueryService` | one of the 9 parallel inspection tasks |
| `farm_manager.service.farm_ai.tool_executor` | reads_from | `farm_manager.service.SenseHubCacheQueryService` | line 16: cache fallback when SenseHub is unreachable |
| `farm_manager.service.farm_level_ai.tool_executor` | reads_from | `collection_blocks` | resolve_block(block_code) -> block document + iotController credentials |
| `farm_manager.service.global_ai.tool_executor` | reads_from | `collection_farms` | resolve_farm_by_name (case-insensitive) |
| `farm_manager.service.global_ai.tool_executor` | reads_from | `collection_blocks` | resolve_block by blockCode |
| `farm_manager.service.SenseHubCacheQueryService` | reads_from | `collection_sensehub_equipment_cache` | stateless cache reads for API + AI tool fallback |
| `farm_manager.service.SenseHubCacheQueryService` | reads_from | `collection_sensehub_alerts_cache` | stateless cache reads for API + AI tool fallback |
| `farm_manager.service.SenseHubCacheQueryService` | reads_from | `collection_sensehub_lab_cache` | stateless cache reads for API + AI tool fallback |
| `farm_manager.service.SenseHubCacheQueryService` | reads_from | `collection_sensehub_snapshots_cache` | stateless cache reads for API + AI tool fallback |
| `farm_manager.service.SenseHubCacheQueryService` | reads_from | `collection_sensehub_sync_log` | stateless cache reads for API + AI tool fallback |
| `farm_manager.service.SenseHubSyncService` | reads_from | `farm_manager.service.BlockRepository` | line 932 (deferred import) |
| `farm_manager.service.block.sensehub_block_service_triggers` | reads_from | `farm_manager.service.PlantDataEnhancedRepository` | line 60 |
| `farm_manager.service.block.sensehub_block_service_triggers` | reads_from | `farm_manager.service.HarvestRepository` | line 181: harvest totals for complete_crop |
| `farm_manager.service.block.expiry_cron` | reads_from | `collection_inventory_harvest` | scans for expiryDate past with availableQuantity > 0 |
| `farm_manager.service.ChemicalsService` | reads_from | `collection_plant_data_enhanced` | discover_from_plant_library walks every fertigation schedule |
