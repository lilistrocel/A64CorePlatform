// Slate theme — re-exports from the canonical user-portal theme
export { theme, lightTheme, darkTheme, buildTheme, themeDark } from './theme';
export type { Theme } from './theme';

// GlobalStyles — the Slate-version (references Slate theme tokens)
export { GlobalStyles } from './GlobalStyles';
