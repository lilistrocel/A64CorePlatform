name: Security Scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Run weekly on Monday at 6 AM UTC
    - cron: '0 6 * * 1'

permissions:
  contents: read
  security-events: write

jobs:
  python-lint:
    name: Python Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          # black is PINNED on purpose. Its stable style changes between
          # releases, so an unpinned install turns this job red across the
          # whole codebase the day a new version lands — through no change of
          # ours. src/ was formatted with exactly this version; bump it
          # deliberately and reformat in the same commit.
          pip install black==26.5.1 flake8

      - name: Check formatting with black
        run: black --check --diff src/

      - name: Lint with flake8
        run: |
          # flake8 is a BUG catcher here, not a style gate — black owns style,
          # and having two tools argue about layout just produces noise nobody
          # reads. Ignored codes and why:
          #
          #   E203  whitespace before ':' — black actively PRODUCES this. The
          #         two tools disagree and black is considered correct; any
          #         repo using both must ignore it or neither can ever pass.
          #   E501  line too long — black already wraps at its own width; the
          #         98 remaining are strings and URLs it cannot split.
          #   F401  imported but unused — 232 hits, and the bulk are
          #         intentional re-exports in __init__.py files that ARE the
          #         module's public API. Removing them breaks imports.
          #   E402  module import not at top — mostly `logger = ...` sitting
          #         between import blocks. Cosmetic.
          #   E741  ambiguous name 'l'. Cosmetic.
          #   F541  f-string without placeholders. Harmless.
          #   F841  assigned but never used — almost all `except ... as e`
          #         where e is unused.
          #
          # Deliberately still ENABLED: F821 (undefined name) and F811
          # (redefinition) — these catch genuine NameErrors and silent symbol
          # shadowing, which is exactly what this job should be for.
          flake8 src/ --max-line-length=120 --exclude=__pycache__,migrations \
            --extend-ignore=E203,E501,F401,E402,E741,F541,F841 \
            --count --show-source --statistics

  python-security:
    name: Python Dependency Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install pip-audit
        run: pip install pip-audit

      - name: Run pip-audit
        run: pip-audit -r requirements.txt --desc --output pip-audit-results.txt || true

      - name: Upload audit results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pip-audit-results
          path: pip-audit-results.txt

  npm-security:
    name: npm Dependency Audit
    runs-on: ubuntu-latest
    defaults:
      run:
        # frontend/ is the npm workspace ROOT (workspaces: shared, user-portal).
        # Only the root has a package-lock.json; installing from the member
        # directory does not work.
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          # Was frontend/user-portal/package-lock.json — a path that has never
          # existed. setup-node's cache step failed and killed the job, so the
          # audit below has not actually run since at least 2026-05.
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Run npm audit
        run: npm audit --audit-level=high || true

  container-scan:
    name: Container Security Scan
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || github.event_name == 'schedule'
    steps:
      - uses: actions/checkout@v4

      - name: Build API image
        run: docker build -t a64core-api:scan .

      - name: Run Trivy on API image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'a64core-api:scan'
          format: 'table'
          exit-code: '0'
          severity: 'CRITICAL,HIGH'
          output: 'trivy-api-results.txt'

      - name: Build Frontend image
        run: docker build -t a64core-frontend:scan -f frontend/user-portal/Dockerfile .

      - name: Run Trivy on Frontend image
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: 'a64core-frontend:scan'
          format: 'table'
          exit-code: '0'
          severity: 'CRITICAL,HIGH'
          output: 'trivy-frontend-results.txt'

      - name: Upload scan results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trivy-scan-results
          path: |
            trivy-api-results.txt
            trivy-frontend-results.txt
